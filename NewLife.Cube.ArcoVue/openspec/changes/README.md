# Changes

存放进行中的变更目录。

**新变更：** `OSC-YYMMDDxxxx <简洁中文描述>/`（例：`OSC-260813c3e9 页面TS抽离与协作编号`）。  
`YYMMDD` = 创建日 Asia/Shanghai；`xxxx` = 4 位随机小写 hex，紧接日期、中间无 `-`。禁止 `max+1` 顺序号。

**历史豁免：** 已存在的 `OSC-0001` … `OSC-0019` 不改名（例：`OSC-0018 实体界面自定义设计方案`）。

已复盘变更整体搬迁至 `archive/`，**保持原文件夹名**。
