# OSC-0003 Retro

> 复盘时间：2026-08-01  
> 触发：验收并复盘 OSC-0003。

## 做得好的

- 用户澄清后按 **A2 加宽** 单号交付，避免「薄 M1」与产品预期错位。
- 微内核落在 ArcoVue `core/` + `views/crud/`，与壳契约隔离清晰。
- 联调驱动缺陷闭环快：列表列、抽屉方向、保存错误可读性均在验收前修完。
- Vitest 覆盖 url/路由/权限/字段/提交/错误格式化，门禁可重复跑。

## 问题与根因

1. **列表无列**：误用 `DataField.visible`（后端默认 false）过滤 GetPage.list。  
2. **抽屉方向**：初版按「左抽屉」产品名做成 `placement=left`，用户要求右侧。  
3. **保存失败不透明**：前端必填用 `required` 而非 `!nullable`；`catch` 吞掉 ApiError。  
4. **文档滞后**：迁移方案一度仍写左抽屉，验收 doc-sync 才改齐。

## 行动项 / lessons

- 已写入 `openspec/harness/lessons.md`（见 OSC-0003 条目）。
- 后续：OSC-0010 补 Menu/Log/LOV/导入导出截图冒烟；OSC-0007/0008 增强历史与评论。

## 与迁移方案偏差

- 原附录 OSC-0003 仅为薄接线；本号按批准的加宽 A2 永久修订 §10.3/§13。
- 「左抽屉」产品措辞改为 **右侧弹出**（实现 + 规则 + 文档）。
