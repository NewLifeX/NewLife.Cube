# OSC-260813397e UI 信息架构

## 登录（飞书风）

```
┌─────────────────────┬──────────────────────┐
│ 背景 loginBackground │ 租户 Code（可选）      │
│ Logo + 系统名        │ 登录 Tabs             │
│ loginTip            │ 表单 + 验证码          │
│ copyright           │ [登录]                 │
│                     │ 忘记密码 · 注册         │
│                     │ —— 其他登录方式 ——      │
│                     │ [OAuth 图标…]          │
└─────────────────────┴──────────────────────┘
```

MFA：右栏替换为「请输入验证器验证码」+ 备用码说明，无左栏变化。

## 账号安全

```
MFA 卡片：状态 / 开启或关闭
第三方：每行 提供商 | 已绑昵称 | 绑定/解绑
```

## 壳

顶栏用户区左侧：租户下拉（EnableTenant 且 items.length>=1 才显示）。
