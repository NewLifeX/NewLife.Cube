# 多维表格与自动化竞品分析报告（ArcoVue vs 飞书 / 钉钉）

> 版本：v1.3 | 日期：2026-08-19 | v1.1 竞品核对；v1.2 补充 Cube 后端元数据增强（不破坏权限契约）；v1.3 §8.6 第 1/2/3/4/6 项立项为 OSC-260819e483

## 1. 概述

本报告对比 NewLife.Cube.ArcoVue 与飞书多维表格、钉钉 AI 表格（原「多维表格 / 智能表格」）在多维视图与自动化两个领域的能力，从产品功能、技术架构、用户交互、扩展能力评估，并输出**企业中后台皮肤**视角的缺口清单。

对比目标不是复刻协作产品，而是吸收对 Cube 管理页有价值的能力。飞书/钉钉是「表格即产品」的数据协作平台；ArcoVue 是「表格即实体管理界面」的 .NET 中后台默认皮肤。

### 1.1 数据来源与验证状态

| 侧 | 数据来源 | 验证状态 |
|----|---------|---------|
| ArcoVue | `web/src` 实测、21 个已归档 OSC + OSC-0018 Draft、[`ArcoVue企业中后台迁移方案.md`](./ArcoVue企业中后台迁移方案.md)（2026-08-18 复审）、[`Doc/功能清单.md`](../Doc/功能清单.md) SPA-7/18/20 | ✅ 代码实测 |
| 飞书多维表格 | 帮助中心 / OpenAPI 字段指南 / 产品介绍（2026-08-19 联网） | ✅ 官方文档核对 |
| 钉钉 AI 表格 | [table.dingtalk.com](https://table.dingtalk.com/)、[帮助中心字段/快速上手](https://help.dingtalk.io/zh/aitable/getting-started/quick-start)（2026-08-19 联网） | ✅ 官方文档核对 |

> 竞品以公开文档为准，个别企业版/灰度能力可能与文档有差。本报告不覆盖飞书/钉钉的云端分享、多租户协作空间、开放平台插件市场——这些与 ArcoVue「自有数据的企业中后台皮肤」定位冲突，不在补齐范围。

### 1.2 ArcoVue 实现基线（2026-08-19 审计）

- **六视图工作台**：table / tree / card / kanban / calendar / gantt。甘特只读（OSC-0019：计划/实际双条重叠、任务条定位图标、表宽拖拽持久化、固定色；`moveable/resizable/scheduleCreatable=false`）。看板/日历同样无拖拽写回（迁移方案 §7.1 / §10.4 设计内「不做」）。
- **命名视图 + ViewProfile 后端权威**：列布局 / 单列服务端排序 / 映射 / 筛选记忆 / 多级分组 / 预定义查询 / 受限表单布局 `FormJson` / 全局只读模板（个人 > 模板 > 系统默认）。
- **查询与筛选两套**：`SearchDrawer` 高级搜索 + `QueriesJson` 预定义查询 → **服务端** `GetList`；筛选构建器 `NamedView.filter`（AND/OR）→ **纯前端过滤当前已加载页**（OSC-0015 架构决策）。
- **右抽屉三 Tab**：表单（OSC-0013 布局）/ 历史（分页 + 动作筛选 Insert/Update/Delete/Automation，无用户/时间筛、无字段 diff）/ 评论（同表回复，最多两级嵌套，无 @提及）。
- **自动化**（OSC-260815fa86）：9 种触发 + 执行器 9 类动作；编辑器「添加动作」菜单 **8 项**（不含 `runAutomation` 入口，旧图仍可执行）。线性 GraphJson + C# `AutomationExecutor`，**不是** FlowGram。
- **字段控件**：`ControlType` 21 种（input / textarea / inputNumber / switch / datePicker / timePicker / select / selectMulti / lov / lovMulti / cascader / upload / image / json / richHtml / richMarkdown / color / icon / email / tel / url / readonly）。列表 LOV / Map 经 `BatchLabel` 显示关联标签，不是查找引用字段。
- **协作与壳**：SSO + 多租户隔离、MFA、顶栏 remind、导入导出、行级 Boolean 徽标启停、列表自定义链接分流（OSC-2608178bdb）。**无** AI 助手浮窗（Cube 后端 AI-7 与 MVC/Vue 皮肤已接线）。
- **测试**：Vitest 逻辑单测（迁移方案 2026-08-18：55 spec / 495 用例；本审查工作区 54 个 `*.spec.ts` + Playwright E2E 3 spec）。**组件测试仍为 0**（无 `@vue/test-utils` / happy-dom）。
- **进行中**：OSC-0018 实体界面自定义设计方案（Draft，纯文档，不改代码）。未创建：OSC-0010 FlowGram 样例、OSC-0011 收口。

### 1.3 v1.0 → v1.1 主要修正

| 原判断 | 修正 | 依据 |
|--------|------|------|
| 钉钉「能力约为飞书的子集」 | 过时。现品牌为 **AI 表格**，官方已具备六视图、仪表盘、工作流、双向关联、公式、查找引用、高级权限、AI 字段 | table.dingtalk.com、help.dingtalk.io |
| 飞书有「树表」一等视图 | ❌。官方六视图为表格 / 看板 / 日历 / 甘特 / 画册 / 表单，无树表 | 飞书帮助「快速上手多维表格」 |
| ArcoVue 画册 ❌、飞书树表 ✅ | 改为：ArcoVue 有 **卡片流**（非以图为主的画册）；飞书/钉钉有画册、无树表 | 代码 OSC-0006/0007；官方视图列表 |
| 「筛选服务端化 ❌」一刀切 | 拆开：高级搜索 / 预定义查询已服务端化；缺口仅筛选构建器 | OSC-0015 design、`effectiveSearch` vs `viewFilter` |
| 8 类动作却列出 9 个名字 | 执行器 9 类；菜单 8 项（无 `runAutomation`） | `automationGraph.ts`、SPA-20 |
| 钉钉 AI 文本 ❌、关联仅单向、仪表盘 ⚠️ | 钉钉官方有 AI 字段/助理、双向关联、仪表盘、工作流 | 钉钉字段表 / 快速上手 |
| 飞书自动化「编辑器内立即执行 ✅」 | 改为 ⚠️。官方路径是沙箱 + 按钮触发 + 运行日志，帮助中心未提供编辑器「立即运行」 | 飞书「使用自动化流程」「使用沙箱」 |
| 公式 / 查找引用列为 P0 | 与迁移方案 **§8.2.6 非目标**冲突；降为「定位外、需先重开决策」 | 迁移方案 §8.2.1 / §8.2.6 |
| 历史「筛时间/用户/动作」 | 仅分页 + 动作筛选；无时间/用户筛 | `useRecordDrawer.ts` |
| 飞书删除触发 ✅ | 飞书工作流/自动化 **不支持删除触发与删除动作**；此点 ArcoVue 反而更全 | 飞书「工作流和自动化常见问题」 |

### 1.4 v1.2 增补

在缺口清单之上增加 **Cube 后端元数据层**分析：哪些缺口前端即可消化、哪些必须改 `GetPage` / `Search` / `DataField` / 日志与评论协议，以及改的时候如何继续遵守菜单权限与 `DataPermission` 契约。详见 §7 / §8。

---

## 2. 竞品概览

| 项目 | 类型 | 技术栈 / 交付 | 定位 | 备注 |
|------|------|--------------|------|------|
| **NewLife.Cube.ArcoVue** | 开源皮肤 | Vue 3 + Arco Design Vue + VisActor VTable；数据在自有 Cube / XCode 后端 | .NET 企业中后台默认皮肤：元数据驱动 CRUD + 多维视图 | 固定容器 + 有限用户运行时自定义；代码级 Section / apps 覆写 |
| **飞书多维表格** | 商业 SaaS | 云端 Bitable | 新一代多维数据协作平台（表格即数据库） | 对标交互范式；自动化 + **工作流画布**双轨；仪表盘独立对象 |
| **钉钉 AI 表格** | 商业 SaaS | 云端；深度绑钉钉通讯录 / 待办 / 审批 | 「AI 时代的应用搭建平台」（2025 起由多维表格升级） | 与飞书能力同级，壁垒在组织通讯录与 OA 闭环，不再是「飞书子集」 |

**定位差异**：飞书/钉钉卖的是多人实时协作的可视化数据库；ArcoVue 卖的是「实体 + `EntityController` + 菜单 → 完整管理页」。吸收清单必须过三道门：① 不破坏 GetPage / Cube 权限契约；② 不引入画布 / Widget 市场（§8.2.1 明确不做）；③ 对企业中后台运维/审计有直接收益。

---

## 3. 功能对比矩阵

图例：✅ 完整　⚠️ 部分可用　❌ 不支持　➖ 不适用（定位不做）　— 无公开对等能力

### 3.1 视图与字段

| 能力 | ArcoVue | 飞书 | 钉钉 | 说明 |
|------|:---:|:---:|:---:|------|
| 表格视图（高性能） | ✅ VTable Canvas | ✅ | ✅ | ArcoVue 万级行流畅 |
| 看板 / 日历 / 甘特 | ✅ 只读（无拖拽写回） | ✅ 可拖拽写回 | ✅ 可拖拽写回 | 写回是协作产品交互；迁移方案定为不做 |
| 树表 | ✅ `treeBuilder` + VTable hierarchy | ❌ 非一等视图 | ❌ 非一等视图 | **ArcoVue 差异化优势**（Parent 实体刚需） |
| 卡片 / 画册 | ⚠️ 卡片流（可映射封面图，非画册产品） | ✅ 画册 | ✅ 画册 | 商品/素材库仍缺以图为主的画册 |
| 表单视图（对外收集） | ❌ | ✅ | ✅ | 外部填报入口；中后台非常规刚需 |
| 仪表盘 / 统计 | ⚠️ `GetChartData` 管线在，`InsightPanel` 暂隐藏 | ✅ 独立仪表盘 | ✅ 独立仪表盘 + AI 仪表盘 | OSC-0016 遗留；迁移矩阵 P1 |
| 命名视图 + 个人配置持久化 | ✅ ViewProfile + 全局模板 | ✅ | ✅ | ArcoVue 此点对齐较好 |
| 字段控件矩阵 | ✅ 21 种 ControlType | ✅ 约 20+ 类型（OpenAPI `type`/`ui_type`） | ✅ 官方 30+（含流程/签名/身份证等） | 飞书「30+」为口碑口径，OpenAPI 枚举约 20 余种 + UI 变体 |
| 公式字段 | ➖ §8.2.6 非目标 | ✅ | ✅ | 见 §6.3 |
| 跨表关联 | ⚠️ Map/LOV 显示关联名（`BatchLabel`），无双向写回 | ✅ 单向 + 双向关联 + 查找引用 | ✅ 单向 + 双向 + 查找引用 + 关联引用 | 列表可读性已部分覆盖；缺「引用关联表其它列」 |
| 人员字段 | ⚠️ LOV / 创建人筛选用 `/Admin/User` 下拉，无头像多人控件 | ✅ 原生多人 | ✅ 原生人员 + 部门 + 群组 | 钉钉与通讯录打通是其壁垒 |
| 附件 / 图片 | ✅ 上传绑定 | ✅ | ✅ | |
| 位置 / 条码 / 流程字段 | ❌ | ✅ | ✅ | 中后台可用 Cascader 地区近似位置 |
| AI 字段 / 字段捷径 | ❌（仅自动化 `aiText` 节点） | ✅ AI 字段捷径 / 侧边栏 | ✅ AI 字段 / 字段 Agent | 前端助手见 §3.3 |

### 3.2 筛选、分组、排序与格式化

| 能力 | ArcoVue | 飞书 | 钉钉 | 说明 |
|------|:---:|:---:|:---:|------|
| 多条件筛选（AND/OR） | ✅ 筛选构建器 | ✅ | ✅ | |
| 关键字 / 字段搜索服务端化 | ✅ `SearchDrawer` + 预定义查询 | ✅ | ✅ | v1.0 漏标 |
| 筛选构建器服务端化 | ❌ 只滤当前已加载页 | ✅ | ✅ | OSC-0015 明确不并入 `GetList`；跨页失真 |
| 多级分组 | ✅ table ≤3 字段 VTable groupBy；树视图禁止 | ✅ | ✅ | |
| 排序 | ⚠️ 表头单列，**服务端**排序并写入 ViewProfile | ✅ 多列 | ⚠️ | 缺多级排序，但不是纯前端 |
| 条件格式 / 行填色 | ❌ | ✅ 填色（单元格/整行/整列，最多 50 条） | ⚠️ 公开文档弱于飞书 | VTable 已有 `cellBgColor`，实现成本低 |
| 行高亮 / 选中样式 | ✅ 主题 token | ✅ | ✅ | |
| 列冻结 | ⚠️ 仅左冻结 | ✅ | ✅ 冻结线拖拽 | 迁移 §10.4 #4：无右冻结 |
| 筛选记忆 + 预定义查询 | ✅ OSC-0012/0016 | ✅ 视图内筛选 | ✅ | ArcoVue 对齐良好 |

### 3.3 自动化与协作

| 能力 | ArcoVue | 飞书 | 钉钉 | 说明 |
|------|:---:|:---:|:---:|------|
| 增 / 改 / 字段变化触发 | ✅ | ✅ | ✅ | |
| **删除触发** | ✅ | ❌ 官方 FAQ 明确不支持 | ⚠️ 文档未强调 | **ArcoVue 优势** |
| 日期到达 / 定时 / 按钮 / Webhook | ✅ | ✅ | ✅ | |
| 通知 / 增改查记录 / HTTP / 延时 | ✅ | ✅ | ✅ | 飞书另有邮件/日程/建群等生态动作 |
| 再执行其它自动化 | ⚠️ 执行器支持 `runAutomation`（深度 ≤3），菜单不提供入口 | ✅ 工作流可编排 | ✅ | 产品决策，防环 |
| AI 生成文本动作 | ✅ `aiText`（无 IAIService 则节点失败） | ✅ | ✅ AI 字段 / 工作流 AI 节点 | v1.0 钉钉标 ❌ 有误 |
| 工作流画布（if/else / 多分支 / 循环） | ❌ OSC-0010 未创建 | ✅ 工作流（与自动化双轨） | ✅ 自动化工作流 | 审批/会签仍建议走 Cube 既有审批，而非复刻画布 |
| 运行日志 | ✅ 编辑器运行日志 Tab | ✅（最长约 7 天） | ✅ 执行记录 | |
| 配置期试跑 | ⚠️ 仅行按钮手动跑；无编辑器「立即执行」 | ⚠️ 沙箱 + 按钮 + 日志 | ⚠️ 执行记录 | 三方都不是「编辑器一键空跑」 |
| 评论（同表回复） | ✅ 最多两级 | ✅ | ⚠️ | |
| 评论 @提及 | ❌ | ✅（含 @智能体） | ⚠️ | |
| 修改历史 | ⚠️ 分页 + 动作筛；Remark 无字段 diff | ✅ 版本/历史 | ⚠️ | 已列 P2；迁移 §3.1 仍写「无分页」已过时 |
| 站内通知 | ✅ 顶栏 remind | ✅ | ✅ 待办/群消息 | |
| AI 助手浮窗 | ❌ ArcoVue 未接线 | ✅ AI 侧边栏 / 问数 | ✅ AI 表格助理 | Cube AI-7 后端现成，MVC/Vue 已有浮窗 |

---

## 4. 非功能对比

| 维度 | ArcoVue | 飞书 | 钉钉 | 评估 |
|------|---------|------|------|------|
| 数据与后端掌控 | ✅ 直连自有库，实体即表 | ❌ 云端锁定 | ❌ 云端锁定 | **核心优势**：合规、审计、复杂业务逻辑可写 C# |
| 权限粒度 | ⚠️ 菜单/按钮 + PERM-6 行级；字段仅 `ReadOnly` 静态元数据；无视图级 | ✅ 表 / 视图 / 字段 / 记录 / 仪表盘 | ✅ 高级权限（角色/字段/记录） | 敏感列可见性是中后台合规缺口 |
| 扩展机制 | ✅ Section（11 Key）+ apps 整页 + 后端控制器自由 | ⚠️ 插件 / API / 应用模式受限 | ⚠️ 插件中心 + 连接平台 | ArcoVue 代码级自由度最高 |
| 多租户 / SSO / MFA | ✅ OSC-260813397e + `/Mfa/*` | ✅ 企业账号体系 | ✅ | 管理端对齐，非协作分享模型 |
| AI 能力 | ⚠️ 后端 AI-7 + 自动化 `aiText`；前端无浮窗 | ✅ 字段捷径 / 仪表盘 AI / AI 搭工作流 | ✅ 字段 Agent / AI 助理 / AI 仪表盘 | 前端接线是最低成本补齐点 |
| 移动端 | ❌ | ✅ | ✅ | 中后台优先级低；审批落地后才有需求 |
| i18n | ❌ 文案未外置（迁移 P0 目标，无书面豁免） | ✅ | ✅ | |
| 自动化测试 | ⚠️ 逻辑单测充分；组件测试 0；E2E 冒烟 3 spec | — | — | 与竞品不可比，但是自身质量门禁 |
| 大数据性能 | ✅ VTable Canvas | ✅ 云端（有行数/自动化次数套餐） | ⚠️ 套餐行数上限，可扩至千万热行 | ArcoVue 不落下风，且无 SaaS 配额 |
| 生态锁定 | ✅ 可换肤（Vue/Naive/MVC） | ❌ 飞书账号 | ❌ 钉钉账号 | |

---

## 5. 与迁移方案的对照（避免把「非目标」写成承诺）

迁移方案权威文件：[`NewLife.Cube.ArcoVue/ArcoVue企业中后台迁移方案.md`](./ArcoVue企业中后台迁移方案.md)（注意：不在 `Doc/Api/` 下）。

| 主题 | 迁移方案结论 | 对竞品分析的约束 |
|------|--------------|------------------|
| 容器模型 §8.2 | 固定 DefaultList，禁止画布 / Widget / 嵌套标签 | 不把飞书「应用模式」当 P0 |
| 洞察区 §8.2.2 / OSC-0016 | 最多一个洞察区；`InsightPanel` 暂隐藏，待简易图表看板 | 仪表盘是**方案内遗留**，不是新立项 |
| §8.2.6 非目标 | 不新增跨实体查询、用户自定义工作流、**公式字段** | 公式 / 双向链接若要做，须先修订方案再立项 |
| 自动化 vs FlowGram | 实体自动化已交付；OSC-0010 只做画布**样例**，不是运行时 | 缺口写「审批画布」时须注明与 C# 执行器双轨 |
| §10.4 仍开放 | 右冻结、角色/租户配置层、i18n、组件测试、`NamedViewsToolbar` 死代码、树列 `__check` 笔误、批量其它操作 | 这些是**自身质量/体验债**，不是飞书功能复刻 |
| OSC-0018 | Draft：开发期自定义方法论 + AI 技能蓝图 | 不改变运行期能力边界 |
| 权限与元数据 | GetPage 唯一字段权威；FormJson 仅呈现 | 后端增强必须服从 §7；字段权限不能靠藏列 |

**矩阵过时点（方案文档自身）**：§3.1「修改历史无分页」已不成立（OSC-0008 已分页）；以代码为准。

---

## 6. 缺口清单

优先级口径：

- **P0**：方案内遗留，或后端已具备、前端未接线，补齐成本低且中后台高频。
- **P1**：显著提升，需单独 OSC；不与 §8.2.6 冲突。
- **P2**：差异化 / 长期。
- **定位外**：竞品有、业务也常见，但方案明示不做——只建议「是否重开决策」，不进入当前迭代承诺。
- **后端列**：`已具备` 前端接线即可；`无需` 纯呈现；`弱` 协议能用但不完整；`需增强` 必须改 Cube 元数据/API 才能安全落地。细则 §8。

### 6.1 🔴 P0 — 方案内或低成本刚需

| # | 缺口 | 现状 | 后端 | 理由 | 与方案关系 |
|---|------|------|:----:|------|------------|
| 1 | **AI 助手浮窗接入** | 无；MVC/Vue 已有 `AiAssistant`，后端 `/Ai/AiChat` | 已具备 | 符合「AI 协作产品」定位；后端 AI-7 现成 | 功能清单 SPA-7 未列，但是 AI-7 对皮肤的对称缺口 |
| 2 | **简易图表看板（启用 InsightPanel）** | 组件仍挂在 `DefaultList` 且 `v-if=false`；`GetChartData` 与列表同源条件已维护 | 弱 | 管理页第一诉求是看总数/趋势；默认 `OnGetChartData` 返回空数组，多数实体无图 | OSC-0016 遗留 |
| 3 | **批量操作工具栏** | 仅批量删除；行内 Boolean 徽标可单条启停；`EnableSelect`/`DisableSelect` 已有 | 弱 | 批量启停可接线；批量改任意字段 **没有** 通用 API | 迁移 §3.1 目标 🟠 P2，启停可提前 |
| 4 | **条件格式 / 行填色** | 无 | 无需 | 逾期红、异常行；VTable `cellBgColor` 已用在分组/甘特；规则可进 `ViewsJson` | 方案未禁止；**不是**权限 |

### 6.2 🟡 P1 — 值得规划

| # | 缺口 | 现状 | 后端 | 理由 | 与方案关系 |
|---|------|------|:----:|------|------------|
| 5 | **筛选构建器服务端化** | 只滤当前页 | 需增强 | 万级跨页失真；默认 `Search` 只有 `Q` + 主时间 + 字段 Equal | OSC-0015 决策需重新立项 |
| 6 | **字段级权限**（视图级见 §8.4） | 仅静态 `ReadOnly` + 行级 PERM-6 | 需增强 | 薪资/成本按角色显隐；`ColumnsJson` 隐藏 **不是** 安全边界 | 需 Cube 后端，皮肤无法独自完成 |
| 7 | **行内编辑（轻量字段）** | 抽屉表单 | 弱 | `PUT` 已走脏字段更新；缺单元格级白名单 PATCH | 必须继续 `EntityAuthorize` + `FindData` |
| 8 | **修改历史字段 diff** | timeline 展示 Remark | 需增强 | `WriteLog(entity)` 写文本，无 `{field,old,new}` | 迁移 P2 |
| 9 | **评论 @提及 + 通知** | 评论 Tab 已接线 | 需增强 | `EntityComment.Content` 纯文本，无提及人 ID | 通知可复用 remind |
| 10 | **i18n + 组件测试** | 文案硬编码；组件测试 0 | 无需 | 迁移 §10.4 #6/#9 | 质量门禁 |
| 11 | **列右冻结** | 仅 left | 无需 | 宽表对照操作列 | §10.4 #4 |
| 12 | **FlowGram 样例** | OSC-0010 从未创建 | 无需 | 画布样例，不替换 C# 执行器 | 迁移 M5 |

### 6.3 ⚪ 定位外 — 先重开 §8.2.6 再立项

| # | 缺口 | 现状 | 后端 | 为何常见 | 建议 |
|---|------|------|:----:|----------|------|
| A | 公式 / 计算列 | 明示不做 | 需增强 | 总价=数量×单价；`ListField.GetValue` 是 MVC 委托且 `JsonIgnore` | 若重开：服务端只读表达式，禁止脚本 |
| B | 查找引用（只读拉关联列） | Map/LOV 显示关联**名称** | 需增强 | 订单行要客户等级/地址，不只客户名 | 双向写回仍不做 |
| C | 对外表单视图 | 无 | — | 报修/报名外部入口 | 独立站点 + 同一实体 API |
| D | 甘特依赖 / 关键路径 | 只读双条已具备舞台 | 需增强 | 工单排期 | 超出只读甘特，要依赖表 |

> v1.0 把 A/B 放进 P0，会与现行方案直接打架。本版改为「决策项」：要做先改迁移方案，再开 OSC。

### 6.4 🟢 P2 — 差异化与长期

| # | 缺口 | 现状 | 后端 | 理由 |
|---|------|------|:----:|------|
| 13 | 画册视图 | 仅卡片流 | 无需 | 依赖图片字段约定 |
| 14 | 人员多选原生控件 | LOV / 用户下拉 | 弱 | 体验问题；数据已是 User 外键 |
| 15 | 自动化监控面板 / 失败率 | 仅运行日志 | 弱 | 聚合查询即可，不必改 GraphJson |
| 16 | 字段控件注册表 | `FieldInput` switch | 无需 | 前端扩展点；`ItemType` 已是开放字符串 |
| 17 | 模板库 / 行业套件 | 无 | 无需 | 后期 |
| 18 | 移动端 | 无 | 无需 | 审批落地后再做 |
| 19 | 角色/租户级 ViewProfile | 仅个人 + UserId=0 模板 | 弱 | **呈现分层**，不要做成第二套数据权限 |

---

## 7. 权限契约（增强元数据时不可破坏）

Cube 的安全模型是「菜单动作 + 行级过滤 + 分区字段集合」，不是飞书式「表/视图/字段/记录」一张高级权限表。任何后端增强必须继续满足：

| # | 契约 | 含义 |
|---|------|------|
| 1 | **GetPage 是 SPA 字段权威** | 某字段是否存在于 list / addForm / editForm / detail / search，只由控制器 `OnGetFields` + XCode `ShowIn` 决定。`FormJson` / `ColumnsJson` / 命名视图只能改顺序、显隐、分组折叠，**不能**增字段、改 `ReadOnly`/`Nullable`/`ItemType`、也不能授权写入。 |
| 2 | **CRUD 权利来自菜单 `PermissionFlags`** | Detail=1、Insert=2、Update=4、Delete=8，由 `EntityAuthorize` 拦在 Action 上。UI 的 canAdd/Edit/Delete 来自 `Auth.Info` / `MenuTree` ∩ `setting.enableAdd` ∩ `!isReadOnly`。不要在 GetPage 里另造一套 ACL 却不在写入路径强制。 |
| 3 | **行级 PERM-6 只在服务端生效** | `DataPermissionAttribute` + 租户 `CreateWhere` 进入 `SearchData` / `FindData`。前端筛选、ViewProfile.filter、预定义查询都不是安全边界。 |
| 4 | **唯一写通道** | `POST`/`PUT`/`DELETE`/`EnableSelect`/`DisableSelect`（及将来的批量改字段）必须 `FindData` + `Valid` + `OnInsert/OnUpdate`。禁止 ViewProfile、筛选 AST、公式表达式变成写入口。 |
| 5 | **列表可见性 = 分区数组成员** | `DataField.Visible` 默认 false 且 `Fill` 从不置 true，**不能**拿它当列表过滤条件。 |
| 6 | **GetPage / GetFields 当前 `[AllowAnonymous]`** | 字段名、类型、LOV 码对未登录也可读。敏感默认值、权限表达式、公式原文若含业务规则，不要塞进匿名元数据；做字段级权限时 GetPage **必须改为按当前用户裁剪**。 |
| 7 | **Map/LOV 是标签，不是可写关联列** | `BatchLabel` / `DataSourceMap` / `Entity.{Type}` 只解决显示名。写回仍是外键本身。 |

---

## 8. Cube 后端元数据需要增强的地方

对照 `DataField` / `GetPage` / 默认 `Search(Pager)`（`ReadOnlyEntityController`、`EntityController`、`FieldCollection`）。下面只列**协议与元数据**缺口：皮肤能独自完成的（填色、右冻结、i18n、AI 浮窗接线）不重复展开。

### 8.1 现有元数据基线（不必推倒）

| 能力 | 已有 | 皮肤侧注意 |
|------|------|------------|
| 五分区字段 | `GetPage`：`setting` + `list`/`addForm`/`editForm`/`detail`/`search` | 搜索分区是 UI 字段清单，真正 WHERE 以 `Search(Pager)` 为准 |
| 字段形状 | `Name`/`DisplayName`/`Category`/`TypeName`/`ItemType`/`Length`/`Nullable`/`PrimaryKey`/`ReadOnly`/`MapField`/`LovCode`/`DataSourceMap` | `Required`、`Authority`、`Extended1~3` 已序列化但 **Fill 未赋值、Cube 未消费** |
| 列表扩展 | `ListField.Url`/`DataAction`/`TextAlign` 等会下发 | `GetValue`/`GetClass`/`DataVisible` 为委托且 `JsonIgnore`，SPA **拿不到** MVC 计算列/行 CSS |
| 行权限 | `DataPermissionAttribute` | 不出现在 GetPage |
| 关联名 | Map `SetRelation`；大表 `LovCode=Entity.*` + `POST /Admin/Lov/BatchLabel` | 只能显示映射字段（通常是名称），不能声明「再带出关联表其它列」 |
| 启停批量 | `GET EnableSelect/DisableSelect?keys=`，需 Update + `Enable` 列 | 不是通用批量改字段 |
| 局部更新 | `PUT` → `CopyFrom(..., true, true)` → XCode 脏字段 | 无独立 PATCH；客户端若提交完整对象，行为取决于模型绑定 |
| 搜索 | `Q`、`dtStart`/`dtEnd`、`{Field}=value` **Equal**、单列 `sort`/`desc` | 控制器重写 `Search` 时常常丢掉通用 Equal（Department 等），这是 OSC-0015 改前端过滤的根因 |
| 图表 | `GET GetChartData`，与列表同一套 `SearchData` | 默认 `OnGetChartData` → `[]`；GetPage **没有**图表元数据 |
| 校验 | `ValidateEntityFields` 用 `!Nullable` + `Length` | WebAPI `EnableFieldValidation` **默认 false**（注释仍写默认 true），等于协议有、门默认关 |
| 审计 | `LogOnChange` → `WriteLog(entity)` | Remark 已是 `Field=old -> new`（P4：前端解析，不改写入） |
| 评论 | `/Cube/EntityComment` 同表回复 | 无提及人列表 |

### 8.2 必须改后端才能安全落地的缺口

#### （1）筛选 AST 进 `SearchData`（缺口 #5）

默认 `Search` 只做关键字、主时间区间、字段等值。筛选构建器的 12 个操作符与 AND/OR **没有**服务端编译器。

增强要点（契约内）：

- 查询增加封闭结构（可复用 `ViewFilter`：`logic` + `conditions[]`），由 `SearchData` 编译为 `WhereExpression`，再 **AND** 到 `CreateWhere()` 之后。数据权限表达式不可被 OR 掉。
- 字段白名单：`Factory.Fields` ∩（GetPage `search` ∪ `list` 中的真实列）。拒绝未知字段、拒绝原始 SQL、`OrderBy` 同样白名单。
- 组合点必须放在 `SearchData` 而不是只改默认 `Search`：大量控制器整段重写 `Search` 且不调用 `base`。应提供 `ApplyRequestFilter(p, ref where)`，重写方必须并入；未并入则文档与单测视为违规。
- 分页在数据库侧完成后再返回。前端 `matchesViewFilter` 可保留为幂等复核，不再是唯一实现。

#### （2）字段级权限（缺口 #6）——真正的元数据缺口

现状：`ReadOnly` 来自 XCode 静态特性；`Authority` 空置；`DataVisible` 不能下发。角色看不见某列，今天只能靠控制器 `RemoveField`（全局）或前端藏列（可被 GetPage/GetDetail/导出绕过）。

增强要点：

- **不要**用 NamedView / `ColumnsJson` 充当 ACL（缺口 #19 同理：角色模板是呈现分层）。
- 按当前用户裁剪五分区：无读权的字段从 list/detail/search 消失；无写权的出现在 detail 但 `ReadOnly=true`，且不进 addForm/editForm。
- 写入路径对称：`Insert`/`Update`/`EnableSelect`/将来的批量改字段/导入/导出，丢弃或拒绝不在本角色 editForm 内的属性。否则前端藏列毫无意义。
- GetPage 改为登录用户上下文输出（匿名只给登录页需要的最小 schema）。现在的 `[AllowAnonymous]` 与「按角色藏薪资列」不能共存。
- 存储上优先「角色 × 实体 × 字段 × 读/写」或控制器可重写的 `FilterFieldsForUser`，避免把权限表达式写进匿名 JSON。

视图级权限（「这个命名视图谁能打开」）建议 **不做**：菜单已经控制谁能进实体；再给 ViewProfile 做 ACL 会变成第二套授权。最多做管理员发布的只读模板（OSC-0014 已有）。

#### （3）通用批量改字段（缺口 #3 的后半）

`EnableSelect` 只覆盖 `Enable`。批量改负责人/状态需要新 Action，但必须逐行 `FindData`（行权）+ 字段 ∈ 当前用户 `editForm` 且 `!ReadOnly`（列权）+ `OnUpdate`（校验与自动化入队）。禁止一条 SQL `UPDATE ... WHERE Id IN (...)` 跳过实体钩子。

#### （4）审计 diff 与评论提及（缺口 #8 / #9）

- Log：`WriteLog("Update", entity)` 已把 Dirtys 写成 `Field=old -> new`。历史 Tab 按字段名解析即可；值含逗号时回退原文。不要再包一层 JSON。
- `EntityComment`：增加提及用户 ID 列表；发表时写 `NotificationRecord`。`Content` 继续纯文本即可，不必上富文本。

#### （5）单元格 PATCH 白名单（缺口 #7）

现有 `PUT` 已能脏更新，行内编辑可以先走它。更稳的契约是 `PATCH { id, values }`：只接受 `editForm` 中非只读字段的子集，先 `FindData` 再 `SetItem`。避免客户端为改一个状态而提交整张表单被 `CopyFrom` 带上未展示字段。

### 8.3 弱项：协议已有、元数据不完整

| 项 | 问题 | 建议 |
|----|------|------|
| 图表（缺口 #2） | GetPage 不声明有哪些图；默认 `OnGetChartData` 为空 | 可选：`setting.charts` 或按枚举/主时间的**约定默认图**（计数、时间趋势），仍走同一 `SearchData`，不引入用户自定义 ECharts option |
| 列表 `stat` | `Index` 把 `p.State` 当作 `TEntity`；`SearchData` 在有数据权限时把 `WhereBuilder` 塞进 `p.State` | 统计对象与权限 builder 分槽，否则洞察区「总数」契约不稳定 |
| 必填 | `Required` 不下发；校验看 `Nullable` 且默认关闭 | `Fill` 时 `Required = !Nullable`；WebAPI 默认打开 `EnableFieldValidation`（与注释一致），子类再关 |
| 多级排序 | Pager 单列 `sort`/`desc` | 接受白名单 `sorts=Name,-Id`，编译为 `OrderBy`；禁止客户端任意 `OrderBy` 字符串 |
| 导入 | CubeNC 有公开 `ImportFile`；WebAPI `EntityController` 导入方法是 protected | SPA 导入若作为产品能力，WebAPI 线需公开 Action，权限与字段白名单同更新 |
| 人员控件（缺口 #14） | 筛选侧已有创建人启发式 + `/Admin/User` | 可选 `ItemType=person` 明示，避免靠字段名正则 |

### 8.4 重开 §8.2.6 才动的元数据（缺口 A / B / D）

这两项一旦做，必须是 **只读投影**，不能变成用户可写的虚拟列。

**只读计算列（A）**

- `ListField.GetValue` 不能进 SPA。需要可序列化的声明：例如 `ItemType=formula` + 封闭表达式（列算术/日期差），服务端在列表/详情求值。
- 计算列强制 `ReadOnly`，不进 addForm/editForm，不进 `CopyFrom`/`PATCH`。
- 禁止任意 JS/SQL。复杂规则继续写 C# 扩展属性（扩展属性本来就会出现在实体 JSON 里）。

**只读查找引用（B）**

- 今天 Map 把外键换成关联显示名；`BatchLabel` 补的也是名称。缺的是「再带 Customer.Level、Customer.Region」。
- 建议在 Map 字段上增加 `LookupDisplays[]`（关联实体字段白名单），GetList/GetDetail **附加**只读列（`CustomerLevel` 等），不替代外键，不提供反向写回。
- 双向链接、在关联表自动建反向列：与实体模型冲突，维持不做。

**甘特依赖（D）**：不是 GetPage 字段问题，而是任务实体要有前置任务外键（或关系表）。有模型之后甘特只读渲染依赖线，写回仍可不做。

### 8.5 明确不要放进元数据的东西

| 不要 | 原因 |
|------|------|
| 把 `NamedView.filter` 当权限 | 用户可清筛选 |
| 条件格式规则当敏感列保护 | 颜色能藏不能防导出 |
| 在 GetPage 下发 `DataPermission` 表达式 | 匿名接口 + 泄露行权规则 |
| 用户自定义工作流 / 公式脚本 / 跨实体查询引擎 | 迁移 §8.2.1 / §8.2.6；写通道会失控 |
| 用 `Authority`/`Extended*` 偷带未文档化语义 | 已空置；安全相关应显式属性 + 写入路径强制 |

### 8.6 建议落地顺序（后端 OSC 视角）

第 1/2/3/4/6 项合并为 **[OSC-260819e483](openspec/changes/OSC-260819e483%20元数据契约与只读投影/proposal.md)**（Draft），`tasks.md` 分阶段 P1–P5。硬约束：不改通用基类方法签名；校验不默认打开；只读投影不改 `DataField` 属性列表。第 5 项仍独立。

1. **契约加固（P1）**：`Required` 在 `PrepareFieldsForApi` 赋值（不改 `Fill`）、校验请求头选择加入（不改 `EnableFieldValidation` 默认 false）、`stat` 安全转换、文档化匿名 GetPage 的边界。
2. **筛选 AST + 现有 Sort（P2）**：解锁缺口 #5。复用 `AutomationFilter` 写入 `p.State`；不新增 `sorts`；前端本号仍单列 `sort`/`desc`（`Pager` 已不绑请求 `OrderBy`）。两栈 EntityTree 内存 `Match`。
3. **批量改字段 + PATCH（P3）**：解锁缺口 #3/#7；仅 WebAPI 新 Action（PATCH/POST + body，不要 GET）+ `FindData`/`editForm` 白名单，不改 PUT/EnableSelect 签名。
4. **字段级 diff + Comment 提及（P4）**：解锁 #8/#9。`LogProvider.WriteLog(entity)` 已写 `Field=old -> new`；本号只解析展示，不改 XCode、不装饰 LogProvider。评论 POST 可选提及 → 现有 `NotificationRecord`，不改 `AddComment`/评论表。
5. **字段级权限**：独立大 OSC（GetPage 鉴权裁剪 + 写入剥离 + 导出）。这是唯一对标飞书「列权限」且不破坏菜单契约的做法。**不在 OSC-260819e483 范围。**
6. **只读 formula / 查找展示 + 用户图表（P5）**：用现有 Map/`lovCode` 与 C# 扩展属性。图表由 InsightPanel 配置 ECharts option，存 ViewProfile `ViewsJson`（`insight.chartOption`），不新增 `autoChart`/GetPage `projections`。修订迁移方案 §8.2.2 / §8.2.3 / §8.2.6。

---

## 9. 结论与建议

**ArcoVue 应继续保持的优势**

1. 数据自主可控，实体即库表，逻辑可写 C#——这是 SaaS 多维表做不到的。
2. 元数据驱动零前端 CRUD + Section/apps 代码级扩展；OSC-0018 正在补「开发期自定义」方法论。
3. 树表是飞书/钉钉都没有的一等视图，贴合组织/分类实体。
4. 自动化触发面不弱于飞书线性自动化（含删除触发、`aiText`），执行器在服务端可编程。
5. VTable 大表性能、命名视图持久化、预定义查询、全局模板，结构层已经对齐。

**差距本质**

结构（多视图、命名视图、搜索、线性自动化）已齐。内容与运营缺口里，**一半是皮肤未接线，一半是 GetPage/Search/Log 协议不够**：

1. **内容可读**：条件格式（前端即可）；洞察图表（用户 option 存 ViewProfile）；只读引用列（须重开方案并扩展 Map）。
2. **可运营**：批量启停（API 已有）；通用批量改与筛选服务端化、字段级权限（必须后端）。
3. **可协作**：AI 浮窗（后端已有）；@提及（通知走 `NotificationRecord`）；历史 diff（解析现有 `Log.Remark`）。审批建议复用 Cube，而非复刻飞书工作流画布。

**建议路线**

1. **立刻可做（不改定位、不改契约）**：AI 助手浮窗、InsightPanel、条件填色、工具栏接 `EnableSelect`/`DisableSelect`、列右冻结。
2. **后端小步（OSC-260819e483，P1–P5）**：Required、校验头、Stat 安全转换、复用 AutomationFilter 的 viewFilter、现有单列 Sort、WebAPI PATCH/批量、解析 Log diff、评论提及通知。图表 option 走 ViewProfile，不浸入 XCode、不改 GetChartData 签名。
3. **后端大步**：字段级权限（GetPage 按用户裁剪，写入/导出对称剥离）。
4. **只读计算列走现有扩展属性（OSC-260819e483 P5）**：查找展示用 Map/`lovCode`；不新增 projections。双向写回与用户脚本仍不做。
5. **明确不做或放样例**：画布编排、双向链接写回、对外收集表单、用 ViewProfile 当数据权限、FlowGram 仅样例（OSC-0010）。

**不要追齐的竞品能力**：云端实时协同编辑、链接分享到互联网、插件市场、钉钉待办/审批生态绑定、按月自动化次数套餐。这些要么与自有数据模型冲突，要么是对方商业壁垒，不是皮肤的职责。
