@using NewLife;
@using NewLife.Web;
@using NewLife.Cube;
@using XCode;
@{
    var fact = ViewBag.Factory as IEntityFactory;
    var fi = fact.MasterTime;
}
@if (fi != null)
{
    @await Html.PartialAsync("_List_Toolbar_DateRange")
}