@using NewLife.Cube.Extensions
@{
    var fact = ViewBag.Factory as IEntityFactory;
    var page = ViewBag.Page as Pager;
}
<div class="d-flex d-inline-flex align-items-center flex-shrink-0">
    @*@Html.ActionLink("访问统计", "Index", "VisitStat", null, new { @class = "btn btn-success btn-sm" })*@
    <label for="category" class="control-label mb-0">类别：</label>
    @Html.ForDropDownList("category", Log.FindAllCategoryName(), page["category"], "全部", true)
    <label for="act" class="control-label mb-0">操作：</label>
    @Html.ForDropDownList("act", Log.FindAllActionName(), page["act"], "全部", true)
</div>
@*@await Html.PartialAsync("_List_Toolbar_SelectUser", new SelectUserModel { Id = "userId", UserId = page["userId"].ToInt(-1) })*@
@await Html.PartialAsync("_List_Toolbar_DateRange")