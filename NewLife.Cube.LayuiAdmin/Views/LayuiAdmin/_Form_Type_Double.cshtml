@model ItemModel
@using NewLife;
@using NewLife.Cube
@using XCode;
@using XCode.Configuration;

@{
    var name = Model.Name;
    var value = Model.Value;
    var type = Model.Type;
}

    <input id="@name" name="@name" type="text" class="layui-input" autocomplete="off" value="@value" />
