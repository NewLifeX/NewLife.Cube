@using NewLife;
@using Microsoft.AspNetCore.Routing
@using XCode;
@using XCode.Configuration;
@{ var item = Model as FieldItem; }
@{ switch (Type.GetTypeCode(item.Type))
    {
        case TypeCode.Boolean:

            break;
    } 
}
