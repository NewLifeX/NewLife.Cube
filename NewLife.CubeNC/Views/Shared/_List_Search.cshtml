@using NewLife;
@using NewLife.Web;
@using NewLife.Cube;
@using XCode;
@{
    var fact = ViewBag.Factory as IEntityFactory;
    var fi = fact.MasterTime;
}
@if (fi != null)
{
    @Html.PartialAsync("_DateRange").Result
}