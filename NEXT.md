---
applyTo: '**'
description: 下一步提示指引
rules:
 - 必须遵守的规则： 解决任务时一定要先分析完成步骤，列出解决方案，并自行评估和选择最优方案，如有疑惑，调用next-step-mcp这个工具的get-next-step获取提示。重要：如果没有问题需要澄清，则可不用调用
 - 必须遵守的规则：根据情况决定是否更新todo，todo的最后两个一定是`总结阶段任务`（不是最终总结，阶段性的总结即可，即每次获取提示后所做工作的总结），另一个是`get-next-step工具获取下一步提示`
 - 必须遵守的规则：**若非要求，禁止进行所有任务的总结**
 - 必须遵守的规则：**如果是需要用户手动验证的问题，通过next-step-mcp这个工具的get-next-step获取验证结果** 
 - 必须遵守的规则：完成任务验证后，请调用next-step-mcp这个工具的get-next-step获取下一步任务或提示 
 - 必须遵守的规则：作为智能编程体，如果有必要，你应该主动调用next-step-mcp这个工具的get-next-step获取下一步提示， 禁止询问用户是否开始或者继续，也禁止手动输入下一步提示，必须通过调用next-step-mcp这个工具的get-next- step获取下一步提示 

- 必须遵守的规则：无论在哪里生成文档，生成文档的命名规则为“日期_时间（精确到分钟）_文件名”,便于排序记录。文档生成位置应根据模块集中管理原则，放在对应模块的docs文件夹下。必要时可以读取相关文档了解项目背景和需求。
- 必须遵守的规则：生成方法的注释和属性的注释<summary>时，注释中的文本调整为1行显示。如：/// <summary> 显示名称与属性映射模型 </summary> ，在修改某个方法或属性时，必须同时修改对应的注释，确保注释内容与代码功能一致。
- 必须遵守的规则：重要！接到任务或任务更新后，应给我一个回复，确认你已收到任务或任务更新，并开始处理。你的分析和解决方案及步骤应尽可能详细反馈，避免遗漏。
- 必须遵守的规则：所有TODO完成后，必须调用next-step-mcp这个工具的get-next-step获取新的任务或提示！
***每次实行完任务，调用next-step-mcp这个工具的get-next-step获取新的任务。

------------TODO List Start------------
（一）任务相关介绍： 
- NewLife.Cube是API版本的魔方
- NewLife.CubeNC是Mvc版本的魔方
（二）更新TODOList并处理以下任务: 
1、帮我找到Cube中发送短信的功能的外部调用的入口，给我具体方法名和所在类名，及调用方法。

------------TODO List End------------
***调用next-step-mcp这个工具的get-next-step获取新的任务。