# NewLife.CubeNC MVC 测试用例清单

> 版本：2026-05-23
> 总计：99 个用例
> 验证手段缩写：UI=页面断言，LOG=日志文件，DB=数据库查询，NET=Network 请求

---

## 优先级说明

| 级别 | 含义 |
|------|------|
| P0 | 核心主流程，必须全部通过，任何一条失败即阻塞发布 |
| P1 | 重要功能，正常迭代必须通过 |
| P2 | 补充验证，可延迟修复 |

---

## Session A — 认证（22 个用例）

> 从无状态开始，依次验证注册、各种登录方式、OAuth 全流程、注销、忘记密码入口。
> 每个场景结束后清除 Cookie，确保后续场景从初始状态开始。

### A.1 注册

| 用例 ID | 优先级 | 标题 | 前置条件 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|---------|
| TC-AUTH-001 | P0 | 用户名注册成功 | 数据库无同名用户 | 1. 打开注册页 `/Admin/User/Register`<br>2. 填写用户名（测试唯一值）、密码（满足强度）、确认密码<br>3. 提交 | UI：跳转到登录页或首页，无报错<br>DB：`User` 表行数 +1，且新用户名存在 |
| TC-AUTH-002 | P0 | 用户名重复注册失败 | TC-AUTH-001 已创建同名用户 | 1. 打开注册页<br>2. 填写与 TC-AUTH-001 相同的用户名<br>3. 提交 | UI：页面显示"用户名已存在"或"用户名重复"提示，不跳转 |
| TC-AUTH-003 | P0 | 邮箱重复注册失败 | 数据库中已有绑定该邮箱的用户 | 1. 打开注册页<br>2. 填写不同用户名但使用已注册邮箱<br>3. 提交 | UI：页面显示邮箱相关错误提示，不跳转 |
| TC-AUTH-004 | P1 | 注册密码以密文传输 | 浏览器可检查 Network | 1. 打开注册页<br>2. 填写表单，在提交前开启 Network 监听<br>3. 点击注册 | NET：POST 请求 body 中存在 `challengeId` 参数，密码字段值为非明文密文 |

### A.2 用户名密码登录

| 用例 ID | 优先级 | 标题 | 前置条件 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|---------|
| TC-AUTH-010 | P0 | 用户名+密码登录成功 | admin 账号存在 | 1. 打开登录页 `/Admin/User/Login`<br>2. 输入用户名 `admin`、密码 `Admin@2025`<br>3. 点击登录 | UI：跳转到后台首页，顶栏显示 admin 用户名 |
| TC-AUTH-011 | P0 | 用户名+错误密码登录失败 | admin 账号存在 | 1. 打开登录页<br>2. 输入用户名 `admin`、错误密码<br>3. 点击登录 | UI：页面保留在登录页，显示错误提示（"用户名或密码错误"） |
| TC-AUTH-012 | P0 | 邮箱+密码登录成功 | admin 绑定了邮箱 | 1. 打开登录页<br>2. 用户名栏输入邮箱地址、正确密码<br>3. 点击登录 | UI：跳转后台首页 |
| TC-AUTH-013 | P0 | 手机号+密码登录成功 | 测试用户绑定了手机号 | 1. 打开登录页<br>2. 用户名栏输入手机号、正确密码<br>3. 点击登录 | UI：跳转后台首页 |
| TC-AUTH-014 | P0 | 用户编码+密码登录成功 | 测试用户有 Code 字段 | 1. 打开登录页<br>2. 用户名栏输入用户编码、正确密码<br>3. 点击登录 | UI：跳转后台首页 |
| TC-AUTH-015 | P1 | 记住密码（Cookie持久化） | 浏览器支持检查 Cookie | 1. 打开登录页<br>2. 勾选"记住密码"<br>3. 输入正确凭据登录 | UI：登录成功<br>NET：Cookie 的 `Expires` 属性 > 当前时间 + 1 天 |
| TC-AUTH-016 | P1 | 登录密码以密文传输 | 浏览器可检查 Network | 1. 打开登录页<br>2. 开启 Network 监听<br>3. 输入正确凭据点击登录 | NET：POST body 中含 `challengeId` 参数且密码字段值为 Base64 密文（非明文） |
| TC-AUTH-017 | P0 | 登录成功跳转正确首页 | admin 账号已登录 | 继 TC-AUTH-010 | UI：URL 为 `/Admin/Index` 或 `/` 或配置的首页路径，无 403/500 |
| TC-AUTH-018 | P0 | 多次连续错误密码触发提示 | 登录失败计数功能开启 | 1. 连续 5 次输入错误密码 | UI：每次均显示错误提示，保持在登录页 |

### A.3 NewLife OAuth 全流程

| 用例 ID | 优先级 | 标题 | 前置条件 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|---------|
| TC-AUTH-020 | P0 | 登录页显示 NewLife 第三方登录按钮 | OAuthConfig 中 NewLife 可见 | 1. 打开登录页 | UI：页面中可见标签含"NewLife"的登录按钮或链接 |
| TC-AUTH-021 | P0 | 点击 NewLife 登录跳转到 OAuth 授权页 | OAuthConfig 配置正确 | 1. 点击 NewLife 登录按钮<br>2. 等待跳转 | UI：浏览器 URL 跳转到 NewLife OAuth 服务的授权页（URL 包含 `oauth` 或 `authorize`） |
| TC-AUTH-022 | P0 | OAuth 授权回跳自动注册新用户 | NewLife OAuth 服务可访问，测试账号 test/test | 1. 在 OAuth 授权页输入 test/test<br>2. 授权确认<br>3. 回跳到 CubeSSO | UI：跳转到后台首页，顶栏显示 OAuth 用户名 |
| TC-AUTH-023 | P0 | OAuth 回跳后 DB 新增用户行 | 继 TC-AUTH-022 | 继 TC-AUTH-022 回跳后 | DB：`User` 表有对应 OAuth 用户的新行<br>DB：`UserConnect` 表有 NewLife 绑定记录 |
| TC-AUTH-024 | P1 | OAuth 回跳后页面无 5xx 错误 | 继 TC-AUTH-022 | 继 TC-AUTH-022 | UI：页面 HTTP 状态码为 200，无错误堆栈信息 |

### A.4 注销

| 用例 ID | 优先级 | 标题 | 前置条件 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|---------|
| TC-AUTH-030 | P0 | 注销跳转到登录页 | 当前已登录 | 1. 点击顶栏"退出"或访问 `/Admin/User/Logout` | UI：跳转到 `/Admin/User/Login` 页面 |
| TC-AUTH-031 | P0 | 注销后访问后台跳回登录页 | 继 TC-AUTH-030 | 1. 直接访问 `/Admin/User` | UI：自动重定向到登录页，返回 302 |

### A.5 忘记密码

| 用例 ID | 优先级 | 标题 | 前置条件 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|---------|
| TC-AUTH-040 | P1 | 忘记密码页面可正常打开 | 无 | 1. 打开登录页<br>2. 点击"忘记密码"链接 | UI：跳转到忘记密码页面，无 404/500 |
| TC-AUTH-041 | P2 | 忘记密码页面包含步骤说明 | 无 | 1. 打开忘记密码页面 | UI：页面中包含验证码发送入口或步骤引导文字 |
| TC-AUTH-042 | P2 | 发送验证码按钮存在 | 忘记密码页面已打开 | 1. 查看忘记密码页面 | UI：页面中有"发送验证码"按钮，可点击（不验证实际发送） |

---

## Session B — 后台菜单全遍历（47 个用例）

> 以 admin 账号登录后保持会话，依次点击全部左侧菜单项。
> 每项验证：内容区可见 + 无 5xx + 有效内容。含搜索条件的菜单验证搜索功能。

### B.1 系统管理区（Admin 区）

| 用例 ID | 优先级 | 菜单标题 | 访问路径 | 特殊验证 |
|---------|--------|---------|---------|---------|
| TC-MENU-001 | P0 | 用户管理 | /Admin/User | UI：表格有数据行（至少 admin）<br>UI：显示用户名和显示名列 |
| TC-MENU-002 | P0 | 角色管理 | /Admin/Role | UI：表格有数据行（至少默认角色） |
| TC-MENU-003 | P0 | 菜单管理 | /Admin/Menu | UI：表格可见，含菜单名称列 |
| TC-MENU-004 | P1 | 部门管理 | /Admin/Department | UI：列表正常加载（可空表，但无 5xx） |
| TC-MENU-005 | P1 | 在线用户 | /Admin/UserOnline | UI：列表正常，当前会话应出现 |
| TC-MENU-006 | P1 | 用户统计 | /Admin/UserStat | UI：列表或图表正常加载 |
| TC-MENU-007 | P1 | 用户令牌 | /Admin/UserToken | UI：列表正常加载 |
| TC-MENU-008 | P1 | 用户链接 | /Admin/UserConnect | UI：列表正常加载 |
| TC-MENU-009 | P1 | 访问规则 | /Admin/AccessRule | UI：列表正常加载 |
| TC-MENU-010 | P1 | OAuth配置 | /Admin/OAuthConfig | UI：列表包含"NewLife"条目 |
| TC-MENU-011 | P1 | OAuth日志 | /Admin/OAuthLog | UI：列表有数据（OAuth 登录后） |
| TC-MENU-012 | P1 | 应用日志 | /Admin/Log | UI：列表有数据（登录操作产生日志） |
| TC-MENU-013 | P1 | 通知记录 | /Admin/NotificationRecord | UI：列表正常加载（可空表） |
| TC-MENU-014 | P2 | 邮件配置 | /Admin/MailConfig | UI：表单或列表页正常加载，无 5xx |
| TC-MENU-015 | P2 | 短信配置 | /Admin/SmsConfig | UI：表单或列表页正常加载，无 5xx |
| TC-MENU-016 | P2 | 数据库工具 | /Admin/Db | UI：页面正常加载，显示数据库连接信息 |
| TC-MENU-017 | P1 | 日志查询 | /Admin/Log | UI：列表有登录日志记录，时间字段非空 |
| TC-MENU-018 | P2 | 参数管理 | /Admin/Parameter | UI：列表正常加载 |
| TC-MENU-019 | P2 | 系统信息 | /Admin/Sys | UI：页面显示版本号、运行时间等系统信息 |
| TC-MENU-020 | P2 | 文件管理 | /Admin/File | UI：列表正常加载 |
| TC-MENU-021 | P2 | 多租户管理 | /Admin/Tenant | UI：列表正常加载 |
| TC-MENU-022 | P2 | 租户用户 | /Admin/TenantUser | UI：列表正常加载 |

### B.2 搜索功能验证（嵌入 Session B 会话）

| 用例 ID | 优先级 | 标题 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|
| TC-MENU-030 | P1 | 用户列表关键字 Q 搜索 | 1. 在用户列表搜索框输入 `admin`<br>2. 点击搜索 | UI：结果仅包含名称含 `admin` 的行，行数 ≥ 1 |
| TC-MENU-031 | P1 | 用户列表时间区间筛选 | 1. 设置 dtStart 为昨天<br>2. 设置 dtEnd 为明天<br>3. 点击搜索 | UI：结果集包含 admin 用户（最后登录时间在范围内） |
| TC-MENU-032 | P1 | 用户列表状态下拉筛选 | 1. 选择状态为"正常"<br>2. 点击搜索 | UI：结果集不包含状态为"禁用"的用户 |
| TC-MENU-033 | P1 | OAuth日志按应用筛选 | 1. 打开 OAuthLog 列表<br>2. 在应用下拉选择 NewLife<br>3. 点击搜索 | UI：结果集仅包含来源为 NewLife 的记录 |
| TC-MENU-034 | P1 | 日志页时间区间搜索 | 1. 打开 Log 列表<br>2. 设置时间范围为最近一小时<br>3. 点击搜索 | UI：结果集包含刚才产生的登录日志 |
| TC-MENU-035 | P1 | 用户列表空搜索返回全量 | 1. 清空搜索条件<br>2. 点击搜索 | UI：结果集包含全部用户，行数 ≥ 1 |

### B.3 魔方管理区（Cube 区）

| 用例 ID | 优先级 | 菜单标题 | 访问路径 | 特殊验证 |
|---------|--------|---------|---------|---------|
| TC-MENU-040 | P1 | 应用系统 | /Cube/App | UI：列表正常，可能有 CubeSSO 自身应用 |
| TC-MENU-041 | P1 | 应用日志 | /Cube/AppLog | UI：列表正常加载（可空表） |
| TC-MENU-042 | P1 | 应用模块 | /Cube/AppModule | UI：列表正常加载 |
| TC-MENU-043 | P1 | 附件管理 | /Cube/Attachment | UI：列表正常加载（可空表） |
| TC-MENU-044 | P1 | 定时作业 | /Cube/CronJob | UI：列表正常，有系统内置作业 |
| TC-MENU-045 | P2 | 委托代理 | /Cube/PrincipalAgent | UI：列表正常加载（可空表） |
| TC-MENU-046 | P2 | 模型表 | /Cube/ModelTable | UI：列表正常加载 |
| TC-MENU-047 | P2 | 模型列 | /Cube/ModelColumn | UI：列表正常加载 |
| TC-MENU-048 | P2 | 区域管理 | /Cube/Area | UI：列表正常加载 |

---

## Session C — 用户信息专项（30 个用例）

> 以 OAuth 登录的测试用户（test/test 回跳注册的账号）身份进入，
> 专项测试用户信息页全部功能。

### C.1 用户显示与入口

| 用例 ID | 优先级 | 标题 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|
| TC-USER-001 | P0 | 后台顶栏显示用户名和头像 | 1. OAuth 用户登录进入后台首页 | UI：顶栏可见用户名文字（非空）；若有头像则可见 `<img>` 标签 |
| TC-USER-002 | P0 | 点击用户名/头像进入用户信息页 | 1. 点击顶栏用户名或头像 | UI：跳转到 `/Admin/User/Info` 页面，无 404/500 |

### C.2 导航栏标签（当前登录用户：10 个标签）

| 用例 ID | 优先级 | 标签名 | 操作步骤 | 验证手段 |
|---------|--------|--------|---------|---------|
| TC-USER-010 | P0 | 基本信息 | 1. 在用户信息页点击"基本信息"标签 | UI：内容区显示用户基本信息表单，含"显示名"字段 |
| TC-USER-011 | P0 | 修改密码 | 1. 点击"修改密码"标签 | UI：内容区显示修改密码表单；因来自 OAuth，页面显示"登录来自 NewLife"或"第三方登录可直接修改密码"提示 |
| TC-USER-012 | P0 | 第三方授权 | 1. 点击"第三方授权"标签 | UI：列表显示 NewLife 对应行，状态为"已绑定" |
| TC-USER-013 | P0 | 租户信息 | 1. 点击"租户信息"标签 | UI：页面正常加载，无 500 |
| TC-USER-014 | P0 | 用户名称（昵称+名称） | 1. 点击用户昵称/名称对应的导航标签（用户 Edit 页） | UI：内容区显示该用户的编辑表单，含昵称（DisplayName）和名称（Name）字段 |
| TC-USER-015 | P0 | 三方链接 | 1. 点击"三方链接"标签 | UI：列表包含 NewLife 对应的绑定条目，Platform 列或 Provider 列可见"NewLife" |
| TC-USER-016 | P0 | 令牌 | 1. 点击"令牌"标签 | UI：列表页正常加载，无 500 |
| TC-USER-017 | P0 | OAuth日志 | 1. 点击"OAuth日志"标签 | UI：列表有 OAuth 登录记录（来自本次 OAuth 登录） |
| TC-USER-018 | P0 | 日志 | 1. 点击"日志"标签 | UI：列表有登录日志记录，包含"登录成功"或相关操作记录 |
| TC-USER-019 | P0 | 通知记录 | 1. 点击"通知记录"标签 | UI：列表页正常加载，可空表，无 500 |

### C.3 基本信息修改

| 用例 ID | 优先级 | 标题 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|
| TC-USER-020 | P0 | 修改显示名并保存 | 1. 在"基本信息"页修改"显示名"为唯一测试值（如 `E2E测试_时间戳`）<br>2. 点击保存 | UI：页面提示保存成功（"操作成功"或绿色提示框）<br>DB：`User` 表对应行 `DisplayName` 字段等于新值 |
| TC-USER-021 | P1 | 修改邮件地址并保存 | 1. 在"基本信息"页修改"邮件"为测试邮箱（唯一值）<br>2. 点击保存 | UI：提示成功<br>DB：`User` 表对应行 `Mail` 字段等于新值 |

### C.4 清空密码流程

> 关键流程：清空密码 → 注销 → 任意新密码登录（新密码自动保存）→ 确认旧密码失效

| 用例 ID | 优先级 | 标题 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|
| TC-USER-030 | P0 | 清空密码后 DB 密码字段为空 | 1. 进入用户信息页<br>2. 点击"清空密码"按钮（通常在 Admin 管理员视图或用户 Edit 页）<br>3. 确认操作 | DB：`User` 表对应行 `Password` 字段为空字符串或 NULL<br>LOG：日志中有"清空密码"相关记录 |
| TC-USER-031 | P0 | 清空密码后用新密码登录成功 | 继 TC-USER-030 | 1. 注销当前会话<br>2. 在登录页输入该用户名 + 任意新密码（如 `NewPass@2026`）<br>3. 点击登录 | UI：跳转到后台首页，登录成功 |
| TC-USER-032 | P0 | 再次注销后旧密码无法登录 | 继 TC-USER-031 | 1. 注销<br>2. 使用 TC-USER-031 之前的旧密码（或空密码）登录 | UI：页面显示登录失败提示，留在登录页 |
| TC-USER-033 | P0 | 新密码已固化可重复登录 | 继 TC-USER-031 | 1. 用 TC-USER-031 的新密码（`NewPass@2026`）登录 | UI：登录成功，跳转首页<br>DB：`User` 表密码字段非空（新密码已保存） |

### C.5 登录统计

| 用例 ID | 优先级 | 标题 | 操作步骤 | 验证手段 |
|---------|--------|------|---------|---------|
| TC-USER-040 | P1 | 多次登录后登录次数递增 | 1. 记录当前登录次数（DB 或用户详情页）<br>2. 注销<br>3. 重新登录 | DB：`User` 表 `Logins` 字段在每次成功登录后递增<br>UI：用户详情页"登录次数"显示值 > 初始记录值 |
| TC-USER-041 | P1 | 最后登录时间更新 | 继 TC-USER-040 登录后 | UI：用户详情页"最后登录"时间显示为刚刚（距当前时间 < 1 分钟）<br>DB：`User` 表 `LastLogin` 字段更新 |
| TC-USER-042 | P1 | 用户详情页显示登录统计信息 | admin 账号已多次登录 | 1. 以 admin 打开用户列表<br>2. 点击 admin 的编辑或详情 | UI：表单中可见"登录次数"和"最后登录"字段，且有非零/非空值 |

---

## 附录：数据库验证索引

下表列出各用例涉及的数据库表和字段，供实现 `DatabaseHelper.cs` 时参考：

| 用例范围 | 数据库文件 | 表名 | 关键字段 |
|---------|----------|------|---------|
| TC-AUTH-001/023 | Membership.db | User | ID, Name, Mail, CreateTime |
| TC-AUTH-023 | Membership.db | UserConnect | UserID, Provider, OpenID |
| TC-AUTH-023 | Membership.db | OAuthLog | UserID, Provider, CreateTime |
| TC-USER-020/021 | Membership.db | User | DisplayName, Mail |
| TC-USER-030 | Membership.db | User | Password |
| TC-USER-033 | Membership.db | User | Password |
| TC-USER-040/041 | Membership.db | User | Logins, LastLogin |

---

## 附录：日志验证关键词

| 用例范围 | 日志关键词 | 说明 |
|---------|----------|------|
| TC-AUTH-010/017 | `登录成功` 或 `Login` | 密码登录成功 |
| TC-AUTH-022 | `OAuth登录` 或 `自动注册` | OAuth 回跳注册 |
| TC-AUTH-030 | `注销` 或 `Logout` | 用户注销 |
| TC-USER-030 | `清空密码` | 管理员清空密码操作 |
| TC-USER-031 | `登录成功` | 新密码登录成功 |
