# 魔方（NewLife.Cube）架构设计

> 版本：2026-07-30（同步更新）  
> 适用范围：NewLife.Cube 第三代（WebAPI 主线）+ NewLife.CubeNC（MVC 兼容层）

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层                                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  MVC Razor 视图  │  │  WebAPI JSON    │  │  前端框架       │  │
│  │  (CubeNC)       │  │  (Cube)         │  │  Vue/React/Blazor│  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                        魔方核心层                                │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────┐  │
│  │ EntityController│ │ 权限系统     │ │  菜单系统     │ │ 配置管理 │  │
│  │ (实体控制器)   │ │ (RBAC)      │ │  (MenuTree)  │ │ (Config)│  │
│  └──────────────┘ └──────────────┘ └──────────────┘ └─────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────┐  │
│  │ OAuth/SSO    │ │ 值集(LOV)   │ │  通知/验证码  │ │ AI 服务  │  │
│  │ 认证服务      │ │ 数据字典     │ │  消息服务     │ │ 抽象层   │  │
│  └──────────────┘ └──────────────┘ └──────────────┘ └─────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐              │
│  │ 嵌入文件服务  │ │ 模块化插件   │ │  Swagger     │              │
│  │ (CubeEmbedded)│ │ (IModule)   │ │  文档         │              │
│  └──────────────┘ └──────────────┘ └──────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│                        数据访问层                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │              XCode ORM (NewLife.XCode)                      ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ ││
│  │  │ 实体模型 │ │ 自动建表 │ │ 二级缓存 │ │ 分表/读写分离    │ ││
│  │  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                        基础设施层                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────────┐  │
│  │ NewLife   │ │ NewLife  │ │ NewLife  │ │ NewLife.Stardust  │  │
│  │ .Core     │ │ .XCode   │ │ .Caching │ │ .Extensions       │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 项目分层

### 2.1 解决方案结构

```
NewLife.Cube (魔方.sln)
├── NewLife.Cube/                    # 第三代魔方核心（WebAPI 版）
│   ├── Areas/Admin/                 # 系统管理区域（用户/角色/菜单/配置）
│   ├── Areas/Cube/                  # 魔方管理区域（附件/定时任务/应用）
│   ├── Controllers/                 # 顶层控制器（认证/SSO/前端接口）
│   ├── Common/                      # 基类（EntityController/ReadOnlyEntityController）
│   ├── Services/                    # 业务服务层
│   ├── Entity/                      # 实体模型
│   ├── Web/                         # OAuth 客户端与网络工具
│   ├── Enums/                       # 枚举定义
│   ├── Extensions/                  # 扩展方法
│   ├── AI/                          # AI 服务集成
│   └── wwwroot/                     # 嵌入式静态文件资源
│
├── NewLife.CubeNC/                  # 第二代魔方（MVC 兼容层）
│   ├── Areas/                       # MVC 版区域
│   ├── Controllers/                 # MVC 版控制器
│   ├── Views/                       # Razor 视图
│   ├── Services/                    # 服务层（与 Cube 共享）
│   └── Jobs/                        # 定时作业
│
├── 主题皮肤项目/                     # MVC 主题包
│   ├── NewLife.Cube.AdminLTE/
│   ├── NewLife.Cube.Metronic/
│   ├── NewLife.Cube.Metronic8/
│   ├── NewLife.Cube.Tabler/
│   ├── NewLife.Cube.LayuiAdmin/
│   └── NewLife.Cube.ElementUI/
│
├── 前端皮肤项目/                     # 第三代前端包
│   ├── NewLife.Cube.Vue/            # Vue3 + Element Plus
│   ├── NewLife.Cube.React/          # React + Ant Design
│   ├── NewLife.Cube.Blazor/         # Blazor 实验
│   ├── NewLife.Cube.Swagger/        # Swagger 文档
│   ├── NewLife.Cube.NaiveUI/        # Vue3 + Naive UI
│   ├── NewLife.Cube.MUI/            # React 19 + MUI 6 (Material)
│   ├── NewLife.Cube.Shadcn/         # React 19 + Radix UI + Tailwind CSS 4
│   ├── NewLife.Cube.ArcoVue/        # Vue3 + Arco Design Vue
│   ├── NewLife.Cube.Angular/        # Angular 19 + NG-ZORRO
│   ├── NewLife.Cube.Vuetify/        # Vue3 + Vuetify 3
│   ├── NewLife.Cube.Svelte/         # SvelteKit 2 + Tailwind CSS 4
│   └── NewLife.Cube.TDesign/        # Vue3 + TDesign
│
├── 演示项目/
│   ├── CubeSSO/                     # SSO 用户中心演示（MVC 版）
│   ├── CubeDemo/                    # WebAPI 版示例
│   └── CubeDemoNC/                  # MVC 版示例
│
└── 测试项目/
    ├── Test/                        # 集成测试
    ├── XUnitTest/                   # 单元测试
    └── E2EMvcTest/                  # E2E 测试
```

### 2.2 核心依赖

| 依赖包 | 用途 | 版本 |
|--------|------|------|
| `NewLife.Core` | 基础库（日志/配置/缓存/序列化/工具） | 11.17+ |
| `NewLife.XCode` | ORM 数据中间件（实体/自动建表/分表/缓存） | 12.0+ |
| `NewLife.Caching` | 缓存抽象（MemoryCache/Redis） | 随 Core |
| `NewLife.IP` | IP 地理信息解析 | 2.4+ |
| `NewLife.Office` | Office 文档处理（Excel 导出） | 1.2+ |
| `NewLife.Stardust.Extensions` | 星尘应用监控与配置中心 | 3.9+ |
| `NewLife.AI` | AI 大模型接入抽象 | 1.4+ |
| `Microsoft.AspNetCore.App` | ASP.NET Core 共享框架 | — |

## 3. 模块架构

### 3.1 BASE：基础服务与扩展（BASE-1 ~ BASE-8）

关联功能清单：BASE-1 统一响应 · BASE-2 权限注解 · BASE-3 模块化插件 · BASE-4 CORS · BASE-5 数据保护 · BASE-6 数据保留 · BASE-7 应用插件 · BASE-8 备份还原

基础服务层提供魔方运行所需的公共能力，包括统一响应协议、声明式权限注解、跨域支持、模块化插件机制等。

### 3.2 AUTH：用户认证与安全（AUTH-1 ~ AUTH-12）

关联功能清单：AUTH-1 密码策略 · AUTH-2 密码登录 · AUTH-3 JWT Token · AUTH-4 Challenge-Response · AUTH-5 图形验证码 · AUTH-6 验证码登录 · AUTH-7 验证码记录 · AUTH-8 用户注册 · AUTH-9 登录风控 · AUTH-10 MFA 多因素认证 · AUTH-11 外部认证集成 · AUTH-12 多设备登录控制

```
┌──────────────────────────────────────────────────────────────┐
│  AuthController (顶层控制器, /Auth/*)                        │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌────────┐ ┌──────┐  │
│  │Login │ │Register│ │Refresh│ │Challenge│ │SendCode│ │Logout│  │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬─────┘ └──┬─────┘ └──────┘  │
│     │        │        │        │          │                   │
├─────┴────────┴────────┴────────┴──────────┴──────────────────┤
│  服务层                                                        │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  UserService (用户服务)                                  │  │
│  │  ┌───────────────────┐ ┌─────────────────────────────┐   │  │
│  │  │ Login/Auth/Register │ │ PasswordService (密码策略)   │   │  │
│  │  └───────────────────┘ └─────────────────────────────┘   │  │
│  │  ┌───────────────────┐ ┌─────────────────────────────┐   │  │
│  │  │ TokenService (JWT) │ │ ExternalAuthService (外部)  │   │  │
│  │  └───────────────────┘ └─────────────────────────────┘   │  │
│  │  ┌───────────────────┐ ┌─────────────────────────────┐   │  │
│  │  │ ICaptchaService   │ │ IMfaService (TOTP)          │   │  │
│  │  └───────────────────┘ └─────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  多设备登录控制                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  CubeSetting.EnableMultiDeviceLogin                     │  │
│  │  true (默认): 注销仅吊销当前会话令牌(jti)                 │  │
│  │  false: 新登录踢旧设备 + 注销吊销全部令牌                  │  │
│  │  UserToken.RevokeByUser(userId) — 按用户批量吊销          │  │
│  │  UserToken.RevokeByTokenId(tokenId) — 按令牌精确吊销      │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  OAuth 客户端                                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  Web/OAuth/ 目录 (10+ 第三方 OAuth 实现)                │  │
│  │  Weixin/DingTalk/GitHub/QQ/Alipay/Microsoft/Baidu/...   │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  SSO 单点登录                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                   │
│  │  SsoController   │  │  SsoClient       │                   │
│  │  (SSO 服务端)    │  │  (SSO 客户端)    │                   │
│  └──────────────────┘  └──────────────────┘                   │
└──────────────────────────────────────────────────────────────┘
```

**多设备登录控制**：通过 `EnableMultiDeviceLogin` 配置（默认 true）。多设备模式下注销仅吊销当前设备令牌（通过 JWT jti 精确定位），不牵连其他设备。单设备模式下新登录自动踢掉旧设备，注销时吊销该账号全部令牌。`ManageProvider.Logout` 已集成该策略判断。管理员可通过 `UserOnlineController.Kick` 强制用户下线（吊销令牌+清除会话），或通过 `UserController.RevokeTokens` 按用户批量吊销。

### 3.3 PERM：权限管理（PERM-1 ~ PERM-8）

关联功能清单：PERM-1 用户管理 · PERM-2 角色管理 · PERM-3 菜单管理 · PERM-4 部门管理 · PERM-5 多租户 · PERM-6 数据权限 · PERM-7 委托代理 · PERM-8 访问规则

```
┌──────────────────────────────────────────────────────────────┐
│  Admin Area 控制器                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │UserCtrl  │ │RoleCtrl  │ │MenuCtrl  │ │TenantCtrl        │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────────────┐  │
│  │Principal │ │AccessRule│ │DataPermissionAttribute        │  │
│  │AgentCtrl │ │Ctrl      │ │(行级数据过滤)                 │  │
│  └──────────┘ └──────────┘ └──────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  权限模型: User → Role → Menu(Action)                        │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  用户 (User) ─── 多对多 ───▶ 角色 (Role)                │  │
│  │  角色 (Role) ─── 多对多 ───▶ 菜单 (Menu) + Action 位    │  │
│  │  菜单 (Menu) ─── 父子关系 ───▶ 子菜单/功能点            │  │
│  └─────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 3.4 DATA：实体控制器（DATA-1 ~ DATA-12）

关联功能清单：DATA-1 标准CRUD · DATA-2 ReadOnly · DATA-3 TreeController · DATA-4 列表配置 · DATA-5 表单配置 · DATA-6 字段元数据 · DATA-7 字段验证 · DATA-8 高级搜索 · DATA-9 数据导入导出 · DATA-10 批量删除 · DATA-11 附件上传 · DATA-12 指令管理

```
┌──────────────────────────────────────────────────────────────┐
│  控制器基类层级                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  ControllerBaseX (所有控制器的基类)                      │  │
│  │  ┌──────────────────────────────────────────────────┐   │  │
│  │  │  EntityController<TEntity, TModel>               │   │  │
│  │  │  ├── ReadOnlyEntityController<TEntity> (只读)    │   │  │
│  │  │  ├── EntityController2<TEntity> (第二代兼容)     │   │  │
│  │  │  └── EntityTreeController<TEntity> (树形)        │   │  │
│  │  └──────────────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  CRUD 路由                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ GET 列表  │ │ GET 详情 │ │POST 新增 │ │ PUT 更新         │  │
│  │ /[area]/  │ │ /Detail │ │ /[area]/ │ │ /[area]/         │  │
│  │ [ctrl]    │ │ ?id=     │ │ [ctrl]   │ │ [ctrl]           │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                      │
│  │DELETE删除 │ │导出数据  │ │GetFields │                      │
│  │?id=1&id=2│ │/Export   │ │字段元数据 │                      │
│  └──────────┘ └──────────┘ └──────────┘                      │
├──────────────────────────────────────────────────────────────┤
│  字段元数据 (驱动前端动态渲染)                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  GetFields 返回四种分类字段:                              │  │
│  │  List(列表列) / Detail(详情) / AddForm(新增) / EditForm  │  │
│  │  每个字段包含: Name/DisplayName/Type/Required/ReadOnly/  │  │
│  │  DataSource/LovCode/Url/Width/Align 等 30+ 属性          │  │
│  └─────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 3.5 SYS：系统管理（SYS-1 ~ SYS-20）

关联功能清单：SYS-1 系统配置 · SYS-2 字典参数 · SYS-3 审计日志 · SYS-4 数据库管理 · SYS-5 系统信息 · SYS-6 IP地理信息 · SYS-7 定时作业 · SYS-8 应用系统管理 · SYS-9 应用日志 · SYS-10 用户在线监控 · SYS-11 用户令牌管理 · SYS-12 用户统计 · SYS-13 地区管理 · SYS-14 附件管理 · SYS-15 文件存储 · SYS-16 值集定义 · SYS-17 值集枚举值 · SYS-18 值集列表配置 · SYS-19 值集搜索字段 · SYS-20 值集表格列

```
┌──────────────────────────────────────────────────────────────┐
│  Admin Area 配套控制器                                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐   │
│  │Config   │ │Log      │ │Parameter│ │Department         │   │
│  │(系统配置) │ │(审计日志) │ │(字典参数)│ │(部门管理)         │   │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐   │
│  │UserOnline│ │UserToken│ │File     │ │Lov (值集管理)     │   │
│  │(在线监控) │ │(令牌管理) │ │(文件存储)│ │(含高级查询)      │   │
│  │(支持Kick) │ │(Revoke) │ │         │ │                   │   │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐   │
│  │Db       │ │XCode    │ │Sys      │ │OAuthConfig         │   │
│  │(数据库)  │ │(实体模型) │ │(系统信息)│ │(OAuth配置)        │   │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                         │
│  │OAuthLog │ │UserConn │ │UserStat │                         │
│  │(OAuth日 │ │(用户链接)│ │(用户统计)│                         │
│  │志)      │ │         │ │         │                         │
│  └─────────┘ └─────────┘ └─────────┘                         │
├──────────────────────────────────────────────────────────────┤
│  Cube Area 配套控制器                                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐   │
│  │CronJob  │ │App      │ │AppLog   │ │Attachment          │   │
│  │(定时作业) │ │(应用系统) │ │(应用日志)│ │(附件管理)         │   │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                         │
│  │Area     │ │Mail/Sms │ │OrderMgr │                         │
│  │(地区管理) │ │(配置)   │ │(指令管理)│                         │
│  └─────────┘ └─────────┘ └─────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

**令牌吊销与强制下线**：`UserOnlineController.Kick` 强制用户下线（吊销令牌 + 清除 Session + 保留审计记录），`UserController.RevokeTokens` 支持运维人员批量吊销用户令牌。`UserToken` 实体提供 `RevokeByUser`（按用户吊销）和 `RevokeByTokenId`（精确吊销单个令牌）方法，由 `ManageProvider.Logout` 根据多设备策略自动调用。
```

### 3.6 OAUTH：统一认证（OAUTH-1 ~ OAUTH-5）

关联功能清单：OAUTH-1 OAuth 第三方登录 · OAUTH-2 SSO 单点登录 · OAUTH-3 OAuth 配置管理 · OAUTH-4 OAuth 日志管理 · OAUTH-5 用户链接管理

统一认证模块提供 OAuth 2.0 第三方登录和 SSO 单点登录能力：
- **OAuth 第三方登录**：对接微信/钉钉/GitHub/QQ/支付宝/微软/百度/淘宝/微博/企业微信等 10+ 平台
- **SSO 单点登录**：统一用户中心，支持 SSO 服务端与客户端模式
- **OAuth 配置管理**：第三方 OAuth 服务商的参数配置与启用/禁用
- **OAuth 日志管理**：OAuth 认证调用的回调日志记录与查看
- **用户链接管理**：用户第三方账号绑定关系管理

> 值集系统（LOV）已并入 SYS（SYS-16 ~ SYS-20），详见 3.5 节。

### 3.7 NOTI：通知与消息（NOTI-1 ~ NOTI-5）

关联功能清单：NOTI-1 邮件发送 · NOTI-2 邮件配置 · NOTI-3 短信发送 · NOTI-4 短信配置 · NOTI-5 站内通知

通知系统采用服务接口 + 数据库记录的方式，通过 `MailService` / `SmsService` 发送消息，记录持久化到数据库供查询。

### 3.8 AI：AI 集成（AI-1 ~ AI-6）

关联功能清单：AI-1 AI 服务接口 · AI-2 AI 日志摘要 · AI-3 AI 安全报告

AI 集成通过 `IAIService` 抽象接口对接大语言模型，提供日志分析、安全巡检和 AI 数据洞察等增值功能。AI 洞察支持 SSE 流式输出、深度推理模式、字段安全过滤，覆盖 MVC 和 Vue 双前端。详见 [AI 洞察架构](#ai-洞察架构)。

### 3.9 MVC：MVC 视图（MVC-1 ~ MVC-8）

关联功能清单：MVC-1 MVC Razor 视图引擎 · MVC-2 MVC 主题皮肤系统 · MVC-3 自定义列表页/表单页/布局 · MVC-4 Tabler 主题 · MVC-5 LayuiAdmin 主题 · MVC-6 Metronic 主题 · MVC-7 ElementUI 主题 · MVC-8 AdminLTE 主题

### 3.10 SPA：SPA 前端（SPA-1 ~ SPA-16）

关联功能清单：SPA-1 WebAPI JSON API · SPA-2 嵌入式静态文件服务 · SPA-3 SPA 回退路由 · SPA-4 Vue 前端皮肤 · SPA-5 React 前端皮肤 · SPA-6 NaiveUI 前端皮肤 · SPA-7 ArcoVue 前端皮肤 · SPA-8 Vuetify 前端皮肤 · SPA-9 TDesign 前端皮肤 · SPA-10 MUI 前端皮肤 · SPA-11 Shadcn 前端皮肤 · SPA-12 Angular 前端皮肤 · SPA-13 Svelte 前端皮肤 · SPA-14 Blazor 前端皮肤 · SPA-15 ECharts 图表集成 · SPA-16 Swagger API 文档

```
┌──────────────────────────────────────────────────────────────┐
│  第二代 (CubeNC) — MVC 服务端渲染                             │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  ThemeViewLocationExpander (主题视图定位器)              │  │
│  │  + CubeEmbeddedFileProvider (嵌入资源文件提供者)         │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                    │  │
│  │  │Admin  │ │Metronic│ │Tabler│ │Layui │ ...              │  │
│  │  │LTE    │ │       │ │      │ │Admin │                    │  │
│  │  └──────┘ └──────┘ └──────┘ └──────┘                    │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  第三代 (Cube) — WebAPI 前后端分离                            │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  CubeController (前端数据接口)                           │  │
│  │  ├── /Cube/MenuTree — 菜单树                             │  │
│  │  ├── /Cube/Avatar — 头像                                 │  │
│  │  ├── /Cube/Image — 图片                                  │  │
│  │  └── /Cube/File — 文件下载                               │  │
│  │                                                          │  │
│  │  前端皮肤包 (NuGet):                                     │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐        │  │
│  │  │Vue   │ │React │ │Blazor│ │Swagger │ │Naive │        │  │
│  │  │+EP   │ │+Antd │ │      │ │ (API)  │ │UI    │        │  │
│  │  └──────┘ └──────┘ └──────┘ └────────┘ └──────┘        │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐        │  │
│  │  │MUI   │ │Shadcn│ │Arco  │ │Angular │ │Vueti│        │  │
│  │  │(Mat) │ │(Rdx) │ │Vue   │ │+NG-    │ │fy   │        │  │
│  │  │      │ │+TW4  │ │      │ │ZORRO   │ │     │        │  │
│  │  └──────┘ └──────┘ └──────┘ └────────┘ └──────┘        │  │
│  │  ┌──────┐ ┌──────┐                                      │  │
│  │  │Svelte│ │TDesig│                                      │  │
│  │  │(Sk2) │ │n     │                                      │  │
│  │  │+TW4  │ │(Vue) │                                      │  │
│  │  └──────┘ └──────┘                                      │  │
│  │                                                          │  │
│  │  嵌入机制:                                               │  │
│  │  wwwroot/ → EmbeddedResource → DLL                       │  │
│  │  CubeEmbeddedFileProvider 提供运行时访问                  │  │
│  │  物理文件优先于嵌入资源（支持覆写）                        │  │
│  └─────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. 关键设计决策

### 4.1 第二代 vs 第三代架构选择

| 决策 | 第二代 (CubeNC) | 第三代 (Cube) |
|------|----------------|---------------|
| 交付模式 | NuGet 包 `NewLife.Cube.Core` | NuGet 包 `NewLife.Cube` |
| 前端渲染 | 服务端 Razor 渲染 | 客户端渲染（JSON API） |
| 视图机制 | ThemeViewLocationExpander | CubeEmbeddedFileProvider |
| 适用场景 | 快速开发、内部系统 | 复杂前端、移动端对接 |
| 演进策略 | 存量维护，不新增功能 | 主力演进，持续迭代 |

### 4.2 文件链接策略

CubeNC 通过 `<Compile Include="..\NewLife.Cube\...">` 链接引用 Cube 项目中的服务层、实体模型、视图模型等公共代码。链接方向为单向（CubeNC → Cube），避免双向依赖。

链接的目录包括：
- `Services/` — 业务服务层
- `Entity/` — 实体模型
- `ViewModels/` — 视图模型
- `Common/` — 公共基类
- `Jobs/` — 定时作业
- `Extensions/` — 扩展方法
- `WebMiddleware/` — 中间件
- `AI/` — AI 服务

### 4.3 统一响应协议

所有 API 接口统一返回 `ApiResponse<T>` 结构：

```json
{
  "code": 0,
  "message": "成功",
  "data": { ... },
  "traceId": "xxx"
}
```

列表接口使用 `ApiListResponse<T>` 扩展分页信息：

```json
{
  "code": 0,
  "data": [ ... ],
  "page": { "pageIndex": 1, "pageSize": 20, "totalCount": 100 },
  "stat": { ... },
  "traceId": "xxx"
}
```

### 4.4 权限模型

魔方采用 Membership 风格的 RBAC 授权模型：

- **用户 (User)**：支持本地用户和 SSO 用户
- **角色 (Role)**：一组菜单与功能动作权限的集合
- **菜单 (Menu)**：可嵌套的树形结构，每个菜单项可关联 URL 和 Action 权限位
- **权限位 (PermissionFlags)**：Detail/Insert/Update/Delete/Export 等

首个用户自动成为管理员，`admin` 名称被禁用。

### 4.5 配置体系

```
CubeSetting (Config<CubeSetting>)
├── 通用设置 (调试/跨域/Cookie/上传/静态资源)
├── 用户登录 (密码/注册/Token/OAuth/SSO/风控)
├── 界面配置 (主题/皮肤/登录页Logo/版权)
└── 功能开关 (AllowLogin/AllowRegister/EnableSms/EnableMail 等)
```

配置存储于数据库参数字典表，支持运行时修改。

### 4.6 模块化插件机制

```
IModule 接口
├── 服务注册 (AddServices)
├── 中间件注册 (Use)
├── 静态资源映射 (MapStaticFiles)
└── 区域注册 (MapArea)

内置模块:
├── CubeService (核心服务注册)
├── VueService (Vue 前端服务)
├── ReactService (React 前端服务)
├── SwaggerService (Swagger 文档)
└── 各主题皮肤模块
```

## 5. 数据流

### 5.1 认证流程

```
用户 → 登录页 → POST /Auth/Login
                    │
                    ▼
              UserService.Login()
                    │
              ┌─────┴─────┐
              │            │
              ▼            ▼
         密码正确        密码错误
              │            │
              ▼            ▼
         生成 JWT      失败计数+1
         Token        达到阈值→封禁
              │
              ▼
         返回 AccessToken
         + RefreshToken
```

### 5.2 实体 CRUD 流程

```
前端请求 → EntityController (API 路由映射)
              │
              ▼
        [EntityAuthorize] 权限检查
              │
              ▼
        执行 CRUD 操作 (OnInsert/OnUpdate/OnDelete)
              │
              ▼
        日志记录 (LogOnChange)
              │
              ▼
        返回 ApiResponse<T>
```

### 5.3 菜单树生成流程

```
用户登录 → 获取角色 → 遍历角色关联的菜单
              │
              ▼
        多角色菜单合并 (去重)
              │
              ▼
        按父子关系构建树形
              │
              ▼
        标记当前用户权限位
              │
              ▼
        返回 MenuTree JSON
```

## 6. 部署视图

```
┌──────────────────────────────────────────────────────────────┐
│  单服务器部署                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  dotnet run (或 IIS/反向代理)                           │  │
│  │  ├── NewLife.Cube.dll (API 核心)                       │  │
│  │  ├── NewLife.Cube.Vue.dll (Vue 前端，嵌入 wwwroot)     │  │
│  │  └── 数据库 (SQLite/MySQL/SQL Server 等)               │  │
│  └─────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  多服务器部署                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │  负载均衡    │→│  API 实例 1  │→│  共享数据库      │    │
│  │  (nginx)     │  ├──────────────┤  │  + Redis 缓存    │    │
│  │              │→│  API 实例 2  │  │  + 文件存储(NFS)  │    │
│  └──────────────┘  └──────────────┘  └──────────────────┘    │
├──────────────────────────────────────────────────────────────┤
│  SSO 多系统部署                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │  系统 A      │  │  系统 B      │  │  SSO 用户中心    │    │
│  │  (魔方)      │  │  (魔方)      │  │  (CubeSSO)       │    │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘    │
│         └──────────────────┴──────────────────┘              │
│                      OAuth 2.0 / JWT                          │
└──────────────────────────────────────────────────────────────┘
```

## 7. 待补充流程

> 以下关键流程需要人工补充时序图或详细说明：

| 编号 | 流程 | 说明 |
|------|------|------|
| F1 | OAuth 第三方登录完整流程 | 从用户点击第三方登录到回跳的完整时序 |
| F2 | 多租户数据隔离实现 | 租户表设计 + 查询拦截实现 |
| F3 | 缓存失效策略 | XCode 二级缓存的失效与更新机制 |
| F4 | 数据导入处理流程 | Excel 上传 → 解析 → 校验 → 写入的完整链路 |
| F5 | 定时作业调度流程 | CronJob 从触发到执行完毕的完整时序 |

---

## 8. 组件清单

| 组件 | 所在项目 | 职责 |
|------|----------|------|
| `EntityController<TEntity,TModel>` | NewLife.Cube/Common | 通用 CRUD 控制器基类 |
| `ReadOnlyEntityController<TEntity>` | NewLife.Cube/Common | 只读实体控制器基类 |
| `EntityTreeController<TEntity>` | NewLife.Cube/Common | 树形实体控制器基类 |
| `AuthController` | NewLife.Cube/Controllers | 统一认证入口 |
| `CubeController` | NewLife.Cube/Controllers | 前端数据接口 |
| `SsoController` | NewLife.Cube/Controllers | SSO 服务端/客户端 |
| `MfaController` | NewLife.Cube/Controllers | 多因素认证 |
| `CubeService` | NewLife.Cube | 魔方启动配置与依赖注入 |
| `CubeSetting` | NewLife.Cube | 魔方全局配置 |
| `UserService` | NewLife.Cube/Services (link) | 用户认证与注册业务 |
| `TokenService` | NewLife.Cube/Services (link) | JWT 令牌服务 |
| `PasswordService` | NewLife.Cube/Services (link) | 密码策略与加密 |
| `ExternalAuthService` | NewLife.Cube/Services (link) | 外部认证集成 |
| `FileStorageService` | NewLife.Cube/Services (link) | 文件存储服务 |
| `DataRetentionService` | NewLife.Cube/Services (link) | 数据保留策略 |
| `DrawingCaptchaService` | NewLife.Cube/Services | 图形验证码 |
| `TotpMfaService` | NewLife.Cube/Services | TOTP 多因素认证 |
| `MailService` | NewLife.Cube/Services | 邮件发送 |
| `SmsService` | NewLife.Cube/Services | 短信发送 |
| `ICaptchaService` | NewLife.Cube/Services | 验证码服务接口 |
| `IMfaService` | NewLife.Cube/Services | MFA 服务接口 |
| `AIService` | NewLife.Cube/AI | AI 大模型接入 |
| `CubeEmbeddedFileProvider` | NewLife.Cube/Extensions (link) | 嵌入式静态文件 |
| `EntityAuthorizeAttribute` | NewLife.Cube/Common | 实体操作权限注解 |
| `DataPermissionAttribute` | NewLife.Cube/Common | 数据权限注解 |
| `MenuAttribute` | NewLife.Cube/Common | 菜单注解 |
| `ApiResponse<T>` | NewLife.Cube/Common | 统一响应模型 |
| `IModule` | NewLife.Cube/Modules (link) | 模块化插件接口 |
| `ModuleManager` | NewLife.Cube/Modules (link) | 模块管理器 |
| `LovAutoRegisterService` | NewLife.Cube/Services | 值集自动注册服务 |
| `LovServiceExtensions` | NewLife.Cube/Services | 值集服务扩展 |
| `PageService` | NewLife.Cube/Services | 页面元数据服务 |
| `AILogSummaryJob` | NewLife.Cube/AI/Jobs | AI 日志摘要定时作业 |
| `AISecurityReportJob` | NewLife.Cube/AI/Jobs | AI 安全报告定时作业 |
| `AccessService` | NewLife.CubeNC/Services (link) | IP 访问规则服务 |
| `JobService` | NewLife.CubeNC/Services (link) | 定时作业服务 |
| `OrderManagerController` | NewLife.Cube/Areas/Cube (link) | 指令管理控制器 |
| `OAuthConfigController` | NewLife.Cube/Areas/Admin (link) | OAuth 配置管理 |
| `OAuthLogController` | NewLife.Cube/Areas/Admin (link) | OAuth 日志管理 |
| `UserConnectController` | NewLife.Cube/Areas/Admin (link) | 用户链接管理 |
| `UserStatController` | NewLife.Cube/Areas/Admin (link) | 用户统计 |
| `BackupDbService` | NewLife.CubeNC/Jobs (link) | 数据库备份服务 |
| `BackupHelper` | NewLife.CubeNC/Jobs (link) | 备份辅助工具 |
| `ApiFilterAttribute` | NewLife.Cube/Common | API 请求过滤器 |
| `ConfigController` | NewLife.Cube/Common | 运行时配置管理控制器 |

---

*架构设计文档由代码结构生成，标注"待补充"的部分需人工补充时序图和详细说明。*

---

## AI 洞察（已合并到 AI 对话助手）

> `AiInsight` Action 已随重构合并到 `AiChat`（AI 对话助手），不再提供独立端点。
> 洞察能力由 `AiChat` 的 `get_data_context` 工具提供；数据收集逻辑保留在 `AiInsightHelper.Collect`。

### 数据收集（保留）

```
CubeTools.get_data_context
  ├── entityId > 0 → GetRecordContext()：当前记录值 + 字段元数据（安全过滤）
  └── 否则 → GetListContext()
      → 克隆 Pager → SearchData() 按列表页逻辑查询当前页数据（数据权限 + 搜索条件 + 排序）
      → AiInsightHelper.Collect<TEntity>() 收集数据
      ├── 字段元数据（安全过滤：名称/ItemType 敏感黑名单）
      ├── 映射字段替换（xxxId → xxxName，与列表页一致）
      └── 当前页数据（仅保留安全字段 + 大文本截断）
  → 返回 JSON 给 LLM 生成分析报告
```

### 核心组件（保留）

| 组件 | 文件 | 说明 |
|------|------|------|
| \`AiDataHelper\` | \`AI/AiDataHelper.cs\` | 字段安全过滤（14 类敏感模式） |
| \`AiInsightHelper\` | \`AI/AiInsightHelper.cs\` | 数据收集（安全过滤）+ 映射字段替换 |

### 已移除

- \`AiInsight\` Action：合并到 \`AiChat\`
- \`IAIService.AnalyzeDataAsync/AnalyzeDataStreamAsync\`：无调用者后删除
- \`AiInsightHelper.BuildPrompt\`：由 LLM 直接消费工具返回数据，不再预构造提示词

---

## AI 对话助手架构

### 数据流

```
右下角悬浮球 → 对话面板（MVC _AiAssistant / Vue AiAssistant.vue）
  → POST /Ai/AiChat（JSON：sessionId + message + 页面上下文 + 目标页面 area/controller）
    → 全局 AiController 统一端点：解析目标控制器（IActionDescriptorCollectionProvider）→ 实例化并初始化
    → 目标实现 IEntityAiContext（实体控制器）→ 校验实体页 Detail 权限 → 构建 CubeTools<TEntity>（SearchData 委托 + 权限）
    → BuildChatSystemPrompt() 注入页面上下文（保留子类重载，经 IEntityAiContext 暴露）
    → AiChatService（NAI 编排：会话历史 ChatSessionService 保持 + 工具循环 + 规范事件流）
      → 产出 SSE JSON 事件 {type:content_delta|tool_call_start|tool_call_done|tool_call_error|run_js|error}
  → 控制器写 SSE → 前端 Markdown 渲染 + 工具卡片 + fill_form 结果填充表单
  → run_js 往返（参考 StarChat ask_user / CheckpointService 架构）：工具经 ToolCallContext 取得检查点编号（优先 ToolCallId）→ SSE 下发 run_js 事件（含 checkpointId + script）→ 前端 new Function(script) 执行 → POST /Ai/OperationResult 回传 → 检查点服务（事件总线广播）解除等待 → 工具返回 LLM 继续
  → get_page_context（页面通用）：目标控制器实现 IPageDataContext 优先服务端取数，否则浏览器采集兜底（复用 run_js 检查点管道）
```

### 核心组件

| 组件 | 文件 | 说明 |
|------|------|------|
| `AiController` | `AI/AiController.cs` | 全局统一端点：`/Ai/AiChat` 解析目标控制器并调用其能力接口（IEntityAiContext/IPageDataContext），另含 OperationResult 回传端点；请求校验/_query 解码/SSE 输出管道（原 AiChatEndpoint）已内聚，文件位于 AI 目录（命名空间保持 Controllers 以稳定菜单权限） |
| `IEntityAiContext` | `AI/IEntityAiContext.cs` | 实体 AI 能力接口：向全局端点暴露 `Factory` / `SearchData` / `CreateCubeTools` / `BuildChatSystemPrompt` 等重载点（仿 IPageDataContext 能力接口模式） |
| `AiChatService`（NAI） | 上游 `NewLife.AI.Services` | NAI 对话编排：会话历史（ChatSessionService）+ 工具循环 + 规范事件流；AiController 每请求创建并复用单例会话服务 |
| `PageCheckpointService` | `AI/PageCheckpointService.cs` | 页面检查点服务：工具挂起等待前端回传（checkpointId + 用户绑定 + 30s 超时），经 `IEventBusFactory` 事件总线广播——单机（MemoryCache）为进程内总线，集群（FullRedis/星尘）跨实例匹配，参考 StarChat CheckpointService |
| `BrowserToolService` | `AI/BrowserToolService.cs` | 浏览器工具服务：run_js 等页面操作工具（检查点编号优先 ToolCallContext.ToolCallId，通道经 CubeBrowserContext 上下文注入或宿主实例属性，virtual 可重写）；另含 `CubeBrowserContext`（浏览器通道上下文） |
| `PageContextCollector` | `AI/PageContextCollector.cs` | 标准浏览器采集脚本：抓取页面标题/表格/表单/`data-ai-context` 扩展钩子，供 get_page_context 浏览器层复用 |
| `IPageDataContext` | `AI/IPageDataContext.cs` | 页面 AI 数据上下文接口：任意页面控制器实现后，get_page_context 优先调用其服务端实现。已落地：`IndexController`（服务器信息）、`DbController`（数据库信息）、`CubeController`（魔方设置，敏感字段过滤） |
| `PageDataContextToolService` | `AI/PageDataContextToolService.cs` | get_page_context 工具：目标控制器实现 IPageDataContext 优先，否则浏览器采集兜底 |
| `IFormAiContext` | `AI/IFormAiContext.cs` | 配置表单 AI 能力接口（仿 IEntityAiContext）：`ConfigController<TConfig>` 实现后，全局端点为配置表单页（魔方设置等）注册 get_form_schema / fill_form 工具；按 `GetMembers` 枚举配置字段（敏感/只读过滤），`BuildFormSystemPrompt` 提供表单提示词 |
| `ConfigFormToolService` | `AI/ConfigFormToolService.cs` | 配置表单工具服务：get_form_schema / fill_form（转发到 IFormAiContext 实现，与实体 `CubeTools` 填表能力对齐） |
| `CubeTools<TEntity>` | `AI/CubeTools.cs` | 实体上下文工具集：get_data_context / get_form_schema（edit 带已有值）/ fill_form（均带中文 [Description] 供 LLM 决策） |
| `SystemInfoToolService` | `AI/SystemInfoToolService.cs` | 系统信息工具服务：get_system_info（服务器指标，实体与全局端点共用同一实现） |
| `CacheChatSessionStore` | `AI/CacheChatSessionStore.cs` | 会话历史存储：基于 `ICacheProvider.Cache`（单机 MemoryCache / 集群 FullRedis 跨节点共享），1 小时过期，替换 NewLife.AI 内置 MemoryCacheSessionStore |
| `AiSessionKey` | `AI/AiSessionKey.cs` | 会话键构建器：按用户+页面+会话三要素生成隔离键（`ai:{userId}:{url}:{sessionId}`），防跨用户/跨页面串话 |
| `AiFormHelper` | `AI/AiFormHelper.cs` | 表单字段 Schema 构建与值类型转换（枚举校验/日期/数值） |
| `_AiAssistant` | `NewLife.CubeNC/Views/Shared/_AiAssistant.cshtml` | MVC 悬浮球 + 对话面板（主题无关样式） |
| `ai-assistant.js` | `NewLife.CubeNC/wwwroot/js/ai-assistant.js` | MVC SSE 消费、Markdown 渲染、工具卡片、表单填充 |
| `AiAssistant.vue` | `NewLife.Cube.Vue/web/core/components/ai/AiAssistant.vue` | Vue 悬浮球 + 对话面板（列表页/表单页上下文） |

### API 端点

```
POST /Ai/AiChat                  # 全局统一端点（所有页面共用；实体页经 IEntityAiContext 提供数据上下文工具，非实体页通用工具）
  body: { sessionId, message, page, mode, id, query(_query Base64), area, controller, url, think }
  → SSE: data: {type:meta|text|tool|run_js|error|done}

POST /Ai/OperationResult   # 全局控制器，所有页面共用，不在各控制器重复增加接口
  body: { checkpointId, result }   # 前端执行 run_js 脚本后回传结果，检查点服务经事件总线广播可跨实例唤醒
```

### 安全设计

- **最小工具集**：仅暴露只读数据收集、表单预填与 run_js 页面操作工具，无写库工具
- **字段安全过滤**：工具复用 `AiDataHelper` 敏感过滤；fill_form 拒绝敏感/自动维护/只读字段
- **权限**：全局 `AiController.AiChat` 要求 `[EntityAuthorize(PermissionFlags.Detail)]`（AI 菜单）；实体页对话额外按目标实体控制器菜单校验 Detail（等价原 `{controller}/AiChat` 语义，经 `IEntityAiContext` 目标解析后校验）；`OperationResult` 同全局权限；AISwitch 由服务统一校验
- **浏览器操作**：run_js 脚本在用户自己浏览器、自己登录会话内执行（等价用户在 DevTools 执行）；`OperationResult` 要求登录且检查点编号绑定当前用户、一次性消费、30 秒超时；脚本内容走 `ToolChatClient` 审计日志
- **审计**：`ToolChatClient.OnToolExecuted` 记录每次工具调用（名称/参数/结果/耗时）
- **会话隔离**：前端按页面生成会话号（sessionStorage + URL 作用域，同页多轮共享、跨页/跨标签页互不干扰）；后端会话键按用户+页面+会话三要素作用域（`AiSessionKey`，防跨用户/跨页面串话）；会话历史存缓存抽象 `ICacheProvider.Cache`（单机 MemoryCache / 集群 FullRedis 跨节点），1 小时过期、限长 30 条
- **数据查询**：列表/记录查询委托由实体控制器提供（SearchData），保留子类重载与数据权限，服务层不绕过

### 扩展点（二次开发）

实体控制器可按需重载以下 `protected virtual` 成员，定制 AI 的提示词与数据逻辑；重载点经 `IEntityAiContext` 能力接口暴露给全局 `AiController` 统一调用：

| 重载点 | 位置 | 作用 |
|--------|------|------|
| `CreateCubeTools(pager, entityId)` | 实体控制器 | 返回自定义 AI 工具集（默认 `CubeTools<TEntity>`） |
| `BuildChatSystemPrompt(req, pager)` | 实体控制器 | 定制 AI 对话系统提示词（页面上下文） |
| `SearchData(pager)` / `Search(pager)` | 实体控制器 | 定制数据查询（含数据权限） |
| `CubeTools<TEntity>` 各工具方法 | AI 工具集 | 工具方法均为 `virtual`：`GetDataContext` / `GetFormSchema` / `FillForm`；数据收集方法 `GetListContext` / `GetRecordContext` 为 `protected virtual`，可继承重写调整数据收集逻辑；也可新增 `[ToolDescription]` 标注的自定义工具 |
| `SystemInfoToolService` 工具方法 | AI 工具服务 | 系统信息工具 `get_system_info` 为 `virtual`，可继承重写调整服务器指标采集逻辑 |
| `BrowserToolService` 工具方法 | AI 工具集 | 浏览器工具方法均为 `virtual`（当前 `run_js`），工具方法接受 `ToolCallContext? context = null`（检查点编号用 ToolCallId，通道经 `CubeBrowserContext` 上下文注入），可继承重写限制脚本或调整执行方式；也可新增 read_element / write_element / click 等页面操作工具 |
| `IPageDataContext` | 任意页面控制器 | 页面实现 `GetPageDataContextAsync()` 为 AI 提供服务端数据上下文；未实现自动降级浏览器采集 |
| `PageDataContextToolService` 工具方法 | AI 工具服务 | `get_page_context` 为 `virtual`，可继承重写调整页面上下文采集逻辑 |

```csharp
// 示例：定制表单 Schema、列表数据收集 + 新增自定义工具
public class MyUserTools : CubeTools<User>
{
    public MyUserTools(IEntityFactory factory, Pager? pager, Int64 entityId, Func<Pager, IList<User>> queryData)
        : base(factory, pager, entityId, queryData) { }

    public override String GetFormSchema(String mode = "add")
        => new { mode, custom = true }.ToJson();

    // 重写数据收集（protected virtual）：调整 get_data_context 的列表数据逻辑
    protected override String GetListContext()
        => new { custom = true, summary = "自定义数据摘要" }.ToJson();

    [ToolDescription("get_my_context", ReadOnly = true)]
    public String GetMyContext() => new { extra = true }.ToJson();
}

// 控制器：重载工具工厂
protected override CubeTools<User> CreateCubeTools(Pager? pager, Int64 entityId)
    => new MyUserTools(Factory, pager, entityId, p => SearchData(p).ToList());
```

> 工具方法的 `[ToolDescription]` 特性支持继承（NewLife.AI ≥ 1.5.2026.804）：重写带特性的 virtual 工具方法后，工具仍会被 `ToolRegistry` 发现并走虚分派到重写实现。

