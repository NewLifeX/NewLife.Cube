# 魔方前端内置架构

## 1. 架构概览

### 终态架构

```
用户 ASP.NET 项目
├── 引用 NewLife.Cube (API核心)            → 控制器/实体/权限/OAuth/服务
├── 引用 NewLife.Cube.Vue (前端皮肤)       → Vue3 + Element Plus 编译产物嵌入
│   或 NewLife.Cube.React (前端皮肤)       → React + Ant Design 编译产物嵌入
│   或 NewLife.Cube.NaiveUI (前端皮肤)     → Vue3 + Naive UI 编译产物嵌入
│   或 NewLife.Cube.MUI (前端皮肤)         → React + Material UI 6 编译产物嵌入
│   或 NewLife.Cube.Shadcn (前端皮肤)      → React + Shadcn UI + Tailwind 编译产物嵌入
└── dotnet run → 浏览器打开即有完整管理后台
```

### 嵌入式 SPA 架构

```
┌─────────────────────────────────────────────────────────────┐
│                    ASP.NET Core 主机                         │
├─────────────────────────────────────────────────────────────┤
│  中间件管道                                                  │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐   │
│  │ 认证/授权    │→│ 静态文件      │→│ 路由 + API 端点    │   │
│  │ JWT Bearer  │  │ Embedded +   │  │ EntityController   │   │
│  │             │  │ Physical     │  │ GetFields/CRUD     │   │
│  └─────────────┘  └──────────────┘  └───────────────────┘   │
│                          ↑                                   │
│              CubeEmbeddedFileProvider                        │
│              (程序集嵌入资源 → HTTP 响应)                      │
├──────────────┬──────────────────────────────────────────────┤
│  Cube.Vue    │  wwwroot/ (嵌入)                              │
│  程序集       │  ├── index.html                              │
│              │  ├── assets/                                  │
│              │  │   ├── index-xxx.js  (Vue 编译产物)         │
│              │  │   └── index-xxx.css                       │
│              │  └── favicon.ico                              │
└──────────────┴──────────────────────────────────────────────┘
```

### 与 MVC 皮肤的对比

| 维度 | MVC 皮肤（AdminLTE/Tabler） | 前端皮肤（Vue/React） |
|------|--------------------------|---------------------|
| SDK | Microsoft.NET.Sdk.Web | Microsoft.NET.Sdk |
| 依赖 | NewLife.CubeNC (MVC) | NewLife.Cube (API) |
| 输出目录 | Bin/ | Bin3/ |
| 包含内容 | Razor Views + wwwroot | wwwroot（编译产物） |
| 渲染方式 | 服务端 Razor | 客户端 SPA |
| 注册方式 | UIService.AddTheme/Skin | 静态文件 + SPA 回退 |
| 前端源码 | 无（直接 CSS/JS） | web/ 目录（需编译） |

## 2. 数据模型

本项目不新增数据实体，复用现有魔方实体体系：

| 实体 | 用途 | 所在项目 |
|------|------|---------|
| User / Role / Menu | 用户权限菜单 | NewLife.Cube (Entity/) |
| App / AppLog | 应用系统与日志 | NewLife.Cube (Entity/) |
| 参数字典 / 地区 | 配置与字典 | NewLife.Cube (Entity/) |

前端通过 `GetFields` API 获取字段元数据，动态渲染 UI，不依赖特定实体结构。

## 3. 接口设计

### 3.1 已有核心接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表查询 | GET | `/{area}/{controller}` | 返回 `ApiListResponse<T>`，含 stat 统计行 |
| 查看详情 | GET | `/{area}/{controller}/Detail?id=` | 返回 `ApiResponse<T>` |
| 字段元数据 | GET | `/{area}/{controller}/GetFields?kind=` | kind=1列表/2详情/3添加/4编辑/5搜索 |
| 新增 | POST | `/{area}/{controller}` | 返回 `ApiResponse<T>` |
| 编辑 | PUT | `/{area}/{controller}` | 返回 `ApiResponse<T>` |
| 删除 | DELETE | `/{area}/{controller}?id=` | 返回 `ApiResponse` |
| 导出 | GET | `/{area}/{controller}/Export{Format}` | Format: Excel/Csv/Json/Xml/Zip |
| 导入 | POST | `/{area}/{controller}/ImportFile` | multipart/form-data 上传文件 |
| 批量删除 | POST | `/{area}/{controller}/DeleteSelect` | keys=id1,id2,... 逗号分隔 |
| 图表数据 | GET | `/{area}/{controller}/GetChartData` | 返回 ECharts option JSON 数组 |

### 3.2 辅助接口（均已实现）

| 接口 | 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|------|
| 菜单树 | GET | `/Admin/Index/GetMenuTree?module=` | 前端动态路由必需 | ✅ 已有 |
| 当前用户 | GET | `/Admin/User/Info` | 用户名/头像/角色/权限 | ✅ 已有 |
| 登录配置 | GET | `/Cube/GetLoginConfig` | OAuth 提供商列表、注册策略 | ✅ 已有 |
| 字典查询 | GET | `/Cube/Lookup?codes=` | 枚举/下拉选项数据源 | ✅ 已有 |
| 页面配置 | GET | `/Cube/GetPageConfig` | 站点名称/Logo/版权等 | ✅ 已有 |
| 系统配置 | GET/PUT | `/Cube/Setting` | 读写 CubeSetting | ✅ 已有 |

### 3.3 GetFields 元数据增强

现有 `DataField` 需确保返回以下字段供前端使用：

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| Name | String | 字段代码名 | 数据绑定 key |
| DisplayName | String | 显示名称 | 表头/标签 |
| TypeName | String | .NET 类型名 | 组件类型推导 |
| ItemType | String | UI 控件类型 | 直接映射前端组件 |
| Width | String | 列宽 | 表格列宽 |
| DataSource | String | 数据源标识 | 下拉选项来源 |
| Required | Boolean | 是否必填 | 表单验证 |
| ReadOnly | Boolean | 是否只读 | 表单控制 |
| Multiple | Boolean | 是否多选 | 搜索字段 |
| Url | String | 链接模板 | 列表点击跳转 |

## 4. 技术选型

| 领域 | 选型 | 理由 |
|------|------|------|
| Vue 前端基础 | QuickVue (V3 + Element Plus + Pinia) | 最成熟，usePage hook 零代码 CRUD，生产级验证 |
| React 前端基础 | CubeAntd (React 18 + Ant Design 5 + UMI Max) | Pro Components 企业级，TypeScript 严格 |
| NaiveUI 前端 | Vue 3 + Naive UI + Vite | 暗黑主题原生支持，TypeScript 友好，轻量高性能 |
| MUI 前端 | React 18 + Material UI 6 + Vite | Google Material Design 风格，全球最大 React UI 库 |
| Shadcn 前端 | React 18 + Shadcn UI + Tailwind CSS + Vite | 极简可定制，代码拥有权，无运行时依赖 |
| 静态文件嵌入 | CubeEmbeddedFileProvider | 已有成熟实现，6 个 MVC 皮肤验证 |
| 模块自动发现 | IModule 接口 | 已有模式，无需额外框架 |
| 前端构建 | Vite (Vue/NaiveUI) / UMI (React) / Vite (MUI/Shadcn) | 各自项目原有工具链 |
| 包管理 | pnpm | 快速、磁盘友好 |
| 公共模块 | @cube/api-core, @cube/field-mapping | pnpm workspace monorepo，跨皮肤共享 |

## 5. 关键设计决策

| 决策点 | 方案 | 备选方案 | 选择理由 |
|--------|------|---------|---------|
| 前端引入方式 | 代码迁移到当前库 | Git 子模块 | 与现有皮肤包模式一致；版本同步；CI 更简单 |
| 项目 SDK | Microsoft.NET.Sdk + FrameworkReference | Microsoft.NET.Sdk.Web | API 版用 plain SDK，保持一致；无 Razor 不需要 Web SDK |
| 输出目录 | Bin3/ | Bin/ | 与 API 核心同目录，NuGet 打包一致 |
| 编译产物 | CI 编译，不提交 Git | 提交到 Git | 仓库干净；前端更新频繁避免 diff 噪音 |
| SPA 回退 | 自定义中间件 | MapFallbackToFile | 嵌入资源不在物理路径，需自定义 |
| 原仓库 | 保留独立维护 | 归档 | 社区可能直接使用独立前端 |

## 6. API 接口差距分析（T005 产出）

### 6.1 P0 关键接口（前端启动必需）—— 全部已有 ✅

| 接口 | 路径 | 所在控制器 | 状态 |
|------|------|----------|------|
| 登录 | `POST /Admin/User/Login` | UserController | ✅ 已有 |
| 当前用户 | `GET /Admin/User/Info` | UserController | ✅ 已有 |
| 菜单树 | `GET /Admin/Index/GetMenuTree?module=` | IndexController | ✅ 已有 |
| 字段元数据 | `GET /{area}/{ctrl}/GetFields?kind=` | ReadOnlyEntityController | ✅ 已有 |
| 列表查询 | `GET /{area}/{ctrl}` | ReadOnlyEntityController | ✅ 已有 |
| 详情 | `GET /{area}/{ctrl}/Detail?id=` | ReadOnlyEntityController | ✅ 已有 |
| 新增 | `POST /{area}/{ctrl}` | EntityController | ✅ 已有 |
| 编辑 | `PUT /{area}/{ctrl}` | EntityController | ✅ 已有 |
| 删除 | `DELETE /{area}/{ctrl}?id=` | EntityController | ✅ 已有 |
| 登录配置 | `GET /Cube/GetLoginConfig` | CubeController | ✅ 已有 |
| 字典查询 | `GET /Cube/Lookup?codes=` | CubeController | ✅ 已有 |
| 页面配置 | `GET /Cube/GetPageConfig` | CubeController | ✅ 已有 |

### 6.2 实体管理控制器 —— 已完整覆盖 ✅

经详细核查，API 版 Admin 和 Cube 区域已拥有 **30+ 个控制器**，与 MVC 版完全对等：

| 区域 | 控制器数 | 代表性控制器 |
|------|---------|------------|
| Admin | 20+ | UserController, RoleController, MenuController, DepartmentController, LogController, ParameterController, OAuthConfigController, UserTokenController, UserConnectController, TenantController 等 |
| Cube | 9 | AppController, CronJobController, AttachmentController, AreaController, ModelTableController 等 |

基类模式分布：
- `EntityController<TEntity, TModel>`：23 个（完整 CRUD + DTO 映射）
- `ReadOnlyEntityController<TEntity>`：4 个（AppLog, XLog, OAuthLog, UserStat）
- `EntityTreeController<TEntity, TModel>`：1 个（Menu）
- `ConfigController<TConfig>`：4 个（CubeSetting, Setting, SysConfig, XCodeSetting）
- `EntityController<TEntity>`：2 个（ModelTable, ModelColumn，实体即 DTO）

**结论**：API 版无控制器缺口，前端可直接全量对接。

### 6.3 皮肤注册方式

经验证，MVC 皮肤（AdminLTE/Tabler 等）均通过**显式调用**注册，非 IModule 自动发现：

```csharp
// CubeDemoNC/Startup.cs
app.UseCube(env);
app.UseAdminLTE(env);   // 显式调用
app.UseTabler(env);     // 显式调用
```

Vue/React 皮肤沿用同模式：

```csharp
// CubeDemo/Program.cs（API 版）
app.UseCube(builder.Environment);
app.UseVue(builder.Environment);    // 一行即启用 Vue 前端
// 或
app.UseReact(builder.Environment);  // 一行即启用 React 前端
// 或
app.UseNaiveUI(builder.Environment);  // Vue3 + Naive UI（暗黑主题）
// 或
app.UseMUI(builder.Environment);      // React + Material UI 6
// 或
app.UseShadcn(builder.Environment);   // React + Shadcn UI + Tailwind
```

IModule 机制作为辅助路径保留，通过 AppModule 管理页可注册到数据库实现免代码启用。

## 7. 任务分解

### 批次 1：基础设施（迭代 1）—— ✅ 已完成

#### 任务 T001：链接 CubeEmbeddedFileProvider 到 API 核心
- **对应功能**：F001
- **输入**：`NewLife.CubeNC\Extensions\CubeEmbeddedFileProvider.cs` 已存在
- **产出**：修改 `NewLife.Cube\NewLife.Cube.csproj`，添加 Compile Link
- **验收**：API 项目编译通过，`NewLife.Cube.Extensions.CubeEmbeddedFileProvider` 可用

#### 任务 T002：创建 NewLife.Cube.Vue 项目骨架
- **对应功能**：F002
- **输入**：AdminLTEService.cs / AdminLTE.csproj 作为模板
- **产出**：
  - `NewLife.Cube.Vue\NewLife.Cube.Vue.csproj`
  - `NewLife.Cube.Vue\VueService.cs`（UseVue 扩展方法 + VueModule : IModule）
  - `NewLife.Cube.Vue\wwwroot\index.html`（占位页面）
  - `NewLife.Cube.Vue\web\README.md`（前端源码说明）
- **验收**：项目编译通过

#### 任务 T003：创建 NewLife.Cube.React 项目骨架
- **对应功能**：F003
- **输入**：同 T002
- **产出**：
  - `NewLife.Cube.React\NewLife.Cube.React.csproj`
  - `NewLife.Cube.React\ReactService.cs`
  - `NewLife.Cube.React\wwwroot\index.html`
  - `NewLife.Cube.React\web\README.md`
- **验收**：项目编译通过

#### 任务 T004：添加项目到解决方案
- **对应功能**：F004
- **输入**：T002, T003 产出的 csproj
- **产出**：修改 `魔方.sln`，新增"前端皮肤"解决方案文件夹
- **验收**：VS 打开解决方案可见新项目，全解决方案编译通过

[检查点 1]：✅ 基础设施就绪，Vue/React 项目骨架可编译，为后续前端迁移提供目标目录。

### 批次 2：API 接口加固（迭代 2）

#### 任务 T005：对比 MVC/API 控制器差距
- **对应功能**：F005
- **产出**：差距分析清单（更新到本文档）
- **结论**：✅ 全部 30+ 控制器已存在于 API 版（详见 6.2 节），无需新增

#### 任务 T006-T009：菜单树/用户信息/GetFields/静态文件中间件
- **结论**：✅ 批次 2 分析确认所有 P0 接口已实现，无需额外开发

[检查点 2]：✅ API 接口完备，CubeDemo 可通过纯 API 完成全部操作。

### 批次 3：前端迁移与联调（迭代 3+4）

#### 任务 T010-T017：前端代码迁移、路径适配、联调、组件补全
- **Vue 迁移**：✅ 186 文件从 QuickVue 复制，修复 element-plus locale 和 @vueuse/core，Vite 构建成功（80 文件 / 4.5 MB）
- **React 迁移**：✅ 146 文件从 CubeAntd 复制，使用国内镜像源安装 2042 依赖包，UMI Max 构建成功（31 文件 / 3.0 MB）
- **嵌入验证**：✅ 程序集 Vue=4.6MB React=3.0MB，全解决方案 Release 编译 0 错误 0 警告

[检查点 3]：✅ 端到端流程走通，Vue/React 前端可用。

### 批次 4：整理与文档（迭代 5）

#### 任务 T018：文件共享重构与清理
- ✅ 修正 package.json name 字段（Vue: QuickVue→Cube.Vue，React: ant-design-pro→Cube.React）
- ✅ 删除 React 项目多余的 `.gitignore copy` 文件
- ✅ 更新两个 web/README.md（标题、介绍、原始项目链接）
- ✅ .gitignore 补充 wwwroot/ 和 pnpm-lock.yaml 排除规则

#### 任务 T019：文档更新
- ✅ 架构文档标记各批次完成状态

#### 任务 T020：内置前端皮肤使用文档
- 详见 Doc/内置前端皮肤.md

[检查点 4]：✅ 代码整洁，文档完善，可对外发布。

### 批次 5：前端功能完善（迭代 6）—— ✅ 已完成

#### 任务 T021-T022：Vue 登录动态配置 + 站点信息
- **产出**：登录页调用 GetLoginConfig 动态渲染、Pinia store 加载站点信息
- ✅ 登录页根据后端配置动态显示/隐藏密码登录、OAuth 按钮
- ✅ 布局组件显示站点名称、Logo、版权信息

#### 任务 T023-T028：Vue 列表页核心功能
- **产出**：搜索/排序/统计行/权限/超链接/表单三态 全面升级
- ✅ 搜索区根据 GetFields(kind=5) 动态渲染
- ✅ 列表排序参数（sort/desc）传递后端
- ✅ 统计行（stat）在列表底部显示（show-summary/summary-method）
- ✅ 权限枚举扩展：Auth.EXPORT=16, Auth.IMPORT=32
- ✅ 字段超链接：URL 变量替换 + SPA router-link + 外链 `<a>` + dataAction AJAX
- ✅ 表单三态：AddForm/EditForm/Detail 使用不同字段集，Detail 只读模式

#### 任务 T029-T032：Vue 导入导出与批量操作
- **产出**：搜索工具栏集成导出/导入/批量删除
- ✅ 导出下拉菜单（Excel/CSV/JSON/XML/模板）权限控制
- ✅ 导入 el-upload 按钮（支持 xlsx/xls/csv/json/zip）权限控制
- ✅ 批量选择 + 批量删除（带确认弹窗，显示选中数量）
- ✅ 清理旧导出逻辑（移除 table2excel、themeConfig 等无用引用）

#### 任务 T033-T035：后端 GetChartData + Vue ECharts
- **产出**：后端图表 API + 前端图表渲染 + dataAction 支持
- ✅ ReadOnlyEntityController 新增 GetChartData action + 虚方法 OnGetChartData
- ✅ Vue Page 组件集成 ECharts（自动加载、响应式、多图表、窗口 resize）
- ✅ 字段 dataAction 支持 AJAX POST 操作后自动刷新列表

#### 任务 T036-T038：React 全面对齐
- **产出**：React 通用 DynamicPage + API 服务 + 通配路由
- ✅ 创建 `services/cube/page.ts` 通用 API 服务（10 个方法）
- ✅ 创建 `DynamicPage/index.tsx` 通用 CRUD 页面（ProTable + 动态列 + ModalForm）
- ✅ 字段类型自动映射（Boolean→switch、DateTime→dateTime、数值→digit）
- ✅ 导出下拉/导入上传/批量删除/ECharts 图表/详情抽屉
- ✅ `config/routes.ts` 新增 `/*` 通配路由，任意菜单路径自动使用 DynamicPage
- ✅ 安装 echarts 6.0 依赖

[检查点 5]：✅ Vue/React 双前端功能全面对齐，覆盖 MVC 版 90%+ 功能。

### 批次 6：CI/CD 自动化 —— ✅ 已完成

#### 任务 T039：GitHub Actions 前端构建集成
- **产出**：publish.yml / publish-beta.yml 工作流支持前端自动编译 + NuGet 打包
- ✅ 两个工作流新增 Node.js 20 + pnpm 9 环境安装步骤
- ✅ Vue 构建步骤：`pnpm install` + `pnpm build`（输出到 wwwroot/）
- ✅ React 构建步骤：`pnpm install` + `npx max build`（输出到 wwwroot/）
- ✅ Pack 阶段新增 `NewLife.Cube.Vue` 和 `NewLife.Cube.React` 打包
- ✅ publish-beta.yml 触发路径新增 `NewLife.Cube.Vue/**` 和 `NewLife.Cube.React/**`

[检查点 6]：✅ CI/CD 全自动：push tag → 前端编译 → .NET 构建 → NuGet 发布。

## 7. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| NuGet 包体积膨胀 | 下载慢，磁盘占用大 | Vite 压缩 + gzip + 拆分 vendor chunk；评估 CDN 可选方案 |
| GetFields 元数据不足 | 前端渲染缺失字段 | 迭代 2 逐字段对齐 MVC 版，参照 QuickVue/CubeAntd 已对接的字段 |
| 前端更新频率高于后端 | NuGet 版本耦合 | 皮肤包使用独立版本号（1.x），可独立发布 |
| CubeEmbeddedFileProvider 跨项目兼容 | 编译可能失败 | T001 优先验证，确保 API SDK 模式下可用 |
| CI 需 Node.js 环境 | 编译复杂度增加 | ✅ 已配置：GitHub Actions setup-node@v4 + pnpm/action-setup@v4 |
| SPA 回退与 API 路由冲突 | 页面白屏或 API 404 | Accept header 检测 + 路径排除规则 |

## 8. 目录结构设计

```
NewLife.Cube/                          # API 核心（已有）
├── NewLife.Cube.csproj               # + 新增 CubeEmbeddedFileProvider Link
├── CubeService.cs
├── Common/                            # EntityController 等
├── Controllers/                       # API 控制器
├── Entity/                            # 业务实体
└── ...

NewLife.Cube.Vue/                      # Vue 前端皮肤包（新增）
├── NewLife.Cube.Vue.csproj           # .NET 库项目壳
├── VueService.cs                     # UseVue() + VueModule
├── wwwroot/                          # 前端编译产物（CI 生成）
│   ├── index.html
│   └── assets/
└── web/                              # Vue3 源码（QuickVue 迁移）
    ├── package.json
    ├── vite.config.ts
    ├── src/
    └── ...

NewLife.Cube.React/                    # React 前端皮肤包（新增）
├── NewLife.Cube.React.csproj
├── ReactService.cs
├── wwwroot/
└── web/                              # React 源码（CubeAntd 迁移）
    ├── package.json
    └── src/

NewLife.Cube.NaiveUI/                  # NaiveUI 前端皮肤包（Phase 1 新增）
├── NewLife.Cube.NaiveUI.csproj
├── NaiveUIService.cs                 # UseNaiveUI() + NaiveUIModule
├── wwwroot/                          # 前端编译产物（CI 生成）
└── web/                              # Vue3 + Naive UI 源码
    ├── package.json
    └── src/

NewLife.Cube.MUI/                      # MUI 前端皮肤包（Phase 1 新增）
├── NewLife.Cube.MUI.csproj
├── MUIService.cs                     # UseMUI() + MUIModule
├── wwwroot/
└── web/                              # React + Material UI 6 源码
    ├── package.json
    └── src/

NewLife.Cube.Shadcn/                   # Shadcn 前端皮肤包（Phase 1 新增）
├── NewLife.Cube.Shadcn.csproj
├── ShadcnService.cs                  # UseShadcn() + ShadcnModule
├── wwwroot/
└── web/                              # React + Shadcn UI + Tailwind 源码
    ├── package.json
    └── src/

packages/                              # 前端公共模块（pnpm workspace）
├── package.json                      # monorepo 根配置
├── pnpm-workspace.yaml
├── api-core/                         # @cube/api-core — 框架无关 API 层
│   ├── package.json
│   └── src/
│       ├── index.ts
│       ├── client.ts                 # axios 封装 + Token 注入
│       ├── token.ts                  # TokenManager
│       └── types.ts                  # ApiResponse/PagedResult/DataField
└── field-mapping/                    # @cube/field-mapping — 字段映射引擎
    ├── package.json
    └── src/
        └── index.ts                  # resolveWidget/resolveWidgets

NewLife.CubeNC/                        # MVC 版（保持不变）
NewLife.Cube.AdminLTE/                 # MVC 皮肤（保持不变）
NewLife.Cube.Tabler/                   # MVC 皮肤（保持不变）
```
