# 魔方登录页接口与功能设计

本文档定义魔方所有前端皮肤登录页的功能需求、接口规范、配置项说明及各皮肤实现细节。

---

## 1. 功能矩阵

| 功能 | 是否受开关控制 | 配置项 |
|------|--------------|-------|
| 密码登录 | 是 | `CubeSetting.AllowLogin` |
| 手机验证码登录 | 是 | `CubeSetting.EnableSms` |
| 邮箱验证码登录 | 是 | `CubeSetting.EnableMail` |
| 第三方（OAuth）登录 | 是，由 OAuthConfig 可见性控制 | `OAuthConfig.Visible` |
| 注册 | 是 | `CubeSetting.AllowRegister` |
| 忘记密码 | 是（依赖短信或邮件通道） | `CubeSetting.EnableSms` / `CubeSetting.EnableMail` |
| Challenge-Response 密码加密 | 是 | `CubeSetting.AllowPlainPassword` |
| 版权信息展示 | 留空不显示 | `CubeSetting.Copyright` |
| 备案号展示 | 留空不显示 | `CubeSetting.Registration` |
| 登录提示 | 留空不显示 | `CubeSetting.LoginTip` |
| 自定义 Logo | 留空使用皮肤内置 | `CubeSetting.LoginLogo` |
| 自定义背景图 | 留空使用皮肤内置 | `CubeSetting.LoginBg` |

### 各皮肤功能覆盖状态

| 皮肤 | 密码登录 | 短信登录 | 邮箱登录 | OAuth | 注册 | 忘记密码 | 加密传输 | 版权 |
|------|---------|---------|---------|-------|------|---------|---------|------|
| Vue (ElementUI) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| ArcoVue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| NaiveUI | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| TDesign | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| React (Ant Design) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Shadcn | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Metronic8 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Metronic (v3) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| ElementUI (MVC) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| LayuiAdmin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 2. 后端接口规范

### 2.1 获取站点信息

```
GET /Cube/SiteInfo
```

**响应示例：**

```json
{
  "code": 0,
  "data": {
    "displayName": "魔方WebApi",
    "copyright": "©2002-2026 NewLife Powered by .NET 9.0.0",
    "registration": "粤ICP备16014330号-1",
    "loginTip": "",
    "logo": "",
    "loginLogo": "",
    "loginBg": ""
  }
}
```

**说明：**
- `copyright`：服务端已替换 `{now:yyyy}` 等变量，前端直接显示（Vue 皮肤用 `v-html` 渲染，支持 HTML 链接）
- `loginLogo` / `loginBg`：为空时前端降级到皮肤内置静态资源

> 旧路径 `/Auth/SiteInfo` 已移除，请使用 `/Cube/SiteInfo`。

### 2.2 获取登录配置

```
GET /Auth/LoginConfig
```

**响应示例：**

```json
{
  "code": 0,
  "data": {
    "displayName": "魔方WebApi",
    "logo": "",
    "allowLogin": true,
    "allowRegister": true,
    "enableSms": false,
    "enableMail": false,
    "loginTip": "",
    "providers": [
      {
        "name": "NewLife",
        "logo": "/Content/images/logo/NewLife.png",
        "nickName": "新生命用户中心"
      }
    ]
  }
}
```

**说明：**
- `providers`：仅返回 `Visible=true` 的 OAuth 提供者
- `providers[].logo`：绝对路径，前端直接作为 `<img src>` 使用，无需处理
- 忘记密码入口是否显示：`enableSms || enableMail` 为 `true` 时才显示（依赖通知通道）

> 旧路径 `/Auth/Config` 保持兼容，内部重定向到 `LoginConfig`，不建议新项目使用。

### 2.3 密码登录

```
POST /Auth/Login
Content-Type: application/json

{
  "username": "admin",
  "password": "...",
  "pkey": ""
}
```

| 字段 | 说明 |
|------|------|
| `password` | 明文密码，或 Challenge-Response 流程加密后的 Base64 密文 |
| `pkey` | Challenge 密钥 ID，非空时后端解密 `password`；不走加密流程时留空 |

**响应：**

```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "accessToken": "...",
    "refreshToken": "...",
    "expireIn": 7200
  }
}
```

### 2.4 获取 RSA 挑战公钥（Challenge-Response）

```
GET /Auth/Challenge
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "pkey": "a3f2c8e1-4b7d-11ef-9012-0800200c9a66",
    "publicKey": "-----BEGIN PUBLIC KEY-----\nMIIBIjAN...\n-----END PUBLIC KEY-----"
  }
}
```

前端用 `publicKey`（PKCS#8 SPKI 格式）以 **RSA-OAEP/SHA-256** 加密密码后，将密文和 `pkey` 一起提交给 `/Auth/Login`。

**安全说明：**
- `pkey` 有效期 300 秒，使用一次后立即失效，防止重放攻击
- 若加密失败（浏览器不支持 Web Crypto API），可降级为明文，此时 `pkey` 不传，但需要服务端 `AllowPlainPassword = true`

### 2.5 发送验证码

```
POST /Auth/SendCode
Content-Type: application/json

{
  "channel": "Sms",
  "username": "13800138000",
  "action": "Login"
}
```

**注意：** `channel` 值区分大小写，手机为 `Sms`，邮箱为 `Mail`。  
`action` 可选值：`Login`（登录）、`Register`（注册）、`ResetPassword`（重置密码）。

**响应：**

```json
{
  "code": 0,
  "data": 123456789
}
```

### 2.6 验证码登录

```
POST /Auth/LoginByCode
Content-Type: application/json

{
  "username": "13800138000",
  "password": "1234",
  "loginCategory": 1
}
```

`loginCategory`：`1` = 手机，`2` = 邮箱。响应格式同 2.3。

### 2.7 忘记密码——重置密码

```
POST /Auth/ResetPassword
Content-Type: application/json

{
  "username": "13800138000",
  "code": "1234",
  "newPassword": "NewPass@2026",
  "confirmPassword": "NewPass@2026"
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `username` | 是 | 手机号或邮箱（与发送验证码时一致） |
| `code` | 是 | 短信/邮件验证码 |
| `newPassword` | 是 | 新密码 |
| `confirmPassword` | 是 | 确认密码，须与 `newPassword` 完全一致 |

调用前须先调用 `POST /Auth/SendCode` 并指定 `action: "ResetPassword"` 发送验证码。

**响应：**

```json
{
  "code": 0,
  "message": "密码重置成功",
  "data": true
}
```

### 2.8 注册

SPA 皮肤（Vue/React 等）：前端跳转到独立 `/register` 路由页面。  
MVC 皮肤（Metronic8 等）：`POST /Admin/User/Register`，Bootstrap Tab 内表单提交。

---

## 3. 配置项说明（CubeSetting）

### 界面配置

| 配置键 | 说明 | 默认值 |
|--------|------|-------|
| `LoginTip` | 登录页提示信息，留空不显示 | 空 |
| `LoginLogo` | 登录页左上角 Logo 图片路径，留空使用皮肤内置 | 空 |
| `LoginBg` | 登录页左侧背景大图路径，留空使用皮肤内置 | 空 |
| `Copyright` | 版权文字，支持 `{now:yyyy}`/`{runtime}`/`{framework}` 变量 | 空（不显示） |
| `Registration` | 备案号，留空不显示 | `沪ICP备10000000号` |

### 用户登录

| 配置键 | 说明 | 默认值 |
|--------|------|-------|
| `AllowLogin` | 允许密码登录 | `true` |
| `AllowRegister` | 允许注册 | `true` |
| `EnableSms` | 启用短信验证码登录（同时控制忘记密码短信通道） | `false` |
| `EnableMail` | 启用邮箱验证码登录（同时控制忘记密码邮件通道） | `false` |
| `AllowPlainPassword` | 允许明文密码传输。设为 `false` 后强制要求前端通过 Challenge-Response 加密密码 | `true` |

> 忘记密码入口依赖通知通道：`EnableSms` 或 `EnableMail` 至少有一个为 `true` 才会在登录页显示"忘记密码"链接。

---

## 4. 前端实现规范

### 4.1 SPA 皮肤（Vue / ArcoVue / NaiveUI / TDesign / React / Shadcn）

#### 登录页结构

```
/login
  ├── 左侧装饰区
  │     ├── Logo（loginLogo 或皮肤内置）
  │     └── 背景图（loginBg 或皮肤内置）
  └── 右侧表单区
        ├── 系统名称
        ├── Tab 标签页
        │     ├── 密码登录（allowLogin=true 显示）
        │     ├── 手机验证码（enableSms=true 显示）
        │     └── 邮箱验证码（enableMail=true 显示）
        ├── 忘记密码链接（enableSms||enableMail=true 显示）
        ├── OAuth 第三方登录（providers 非空时显示，分割线 + 图标列表）
        ├── 注册入口（allowRegister=true 显示）
        └── 底部版权信息（copyright / registration）

/forgot-password
  ├── Step 1：输入手机号或邮箱，选择接收方式，发送验证码
  └── Step 2：输入验证码 + 新密码 + 确认密码，提交重置
        └── 成功后跳转到 /login

/register
  └── 注册表单（用户名、邮箱、密码、确认密码）
```

#### Challenge-Response 加密流程（所有皮肤通用，由 `@cube/auth-logic` 封装）

```typescript
// packages/auth-logic/src/index.ts（login 函数节选）
async function login(username: string, password: string) {
  let payload = { username, password, pkey: '' };
  try {
    const cr = await api.get('/Auth/Challenge');
    if (cr.data?.publicKey) {
      const encrypted = await encryptPassword(password, cr.data.publicKey);
      if (encrypted) {
        payload.password = encrypted;
        payload.pkey = cr.data.pkey;
      }
    }
  } catch {
    // 降级：Challenge 失败时继续使用明文（需服务端 AllowPlainPassword=true）
  }
  return api.post('/Auth/Login', payload);
}
```

#### OAuth 按钮渲染细节

- 图标：`<img :src="item.logo" />`，`item.logo` 由后端返回绝对路径，无需前端处理
- 文字：优先 `item.nickName`，降级到 `item.name`
- 登录跳转：`window.location.href = '/Sso/Login/' + item.name + '?r=' + encodeURIComponent(returnUrl)`（需全页刷新完成 OAuth 流）
- `returnUrl` 默认为登录后拟访问的页面，若无则为首页

#### 版权渲染细节

- `copyright` 字段已由服务端替换变量（含 HTML 链接），前端使用 `v-html`（Vue）或 `dangerouslySetInnerHTML`（React）渲染
- `registration` 字段为纯文本，包裹在 `<a href="https://www.beianx.cn/">` 链接中

#### 验证码发送逻辑

1. 点击"发送验证码"按钮，调用 `POST /Auth/SendCode`
2. 成功后开始 60 秒倒计时，按钮禁用并显示剩余秒数
3. 倒计时结束后按钮恢复可点击状态
4. 失败时显示错误信息，按钮保持可用

### 4.2 MVC 皮肤（Metronic8 / Metronic v3 / ElementUI / LayuiAdmin）

- Tab 结构使用 Bootstrap 的 `data-toggle="tab"` 切换
- 验证码发送使用 jQuery AJAX 调用 `/Admin/User/SendVerifyCode`（原有 MVC 端点）
- 注册使用 Bootstrap Tab 切换到本页内的注册面板（`#Register`）
- 忘记密码使用 Bootstrap Tab 切换到本页内的忘记密码面板（`#ForgotPassword`），两步骤通过 JS 控制显示/隐藏
- 版权信息调用 `config.GetCopyright()` 方法渲染

#### MVC 皮肤忘记密码面板流程

```
点击"忘记密码"链接
  └── Bootstrap Tab 切换到 #ForgotPassword 面板（Step 1）

Step 1：输入手机号或邮箱，点击发送验证码
  └── AJAX POST /Auth/SendCode { channel, username, action: "ResetPassword" }
        └── 成功 → 显示 Step 2（输入验证码和新密码）

Step 2：输入验证码 + 新密码 + 确认密码，点击提交
  └── AJAX POST /Auth/ResetPassword { username, code, newPassword, confirmPassword }
        ├── 成功 → 切换回登录 Tab，显示"密码重置成功，请登录"提示
        └── 失败 → 显示错误信息在当前 Step 2
```

#### MVC 皮肤 Challenge-Response 实现

MVC 皮肤登录表单提交时，通过内联 JS 函数拦截 `onsubmit` 事件完成加密：

```javascript
// 表单结构（含 pkey 隐藏字段）
<form id="login_form" onsubmit="return handleLoginSubmit(event)">
  <input type="hidden" name="pkey" id="pkey_input" value="" />
  <input type="password" name="password" id="password_field" />
</form>

// 加密并提交
async function handleLoginSubmit(e) {
  e.preventDefault();
  try {
    const resp = await fetch('/Auth/Challenge');
    const { data } = await resp.json();
    if (data?.publicKey) {
      const encrypted = await encryptRSA(document.getElementById('password_field').value, data.publicKey);
      if (encrypted) {
        document.getElementById('password_field').value = encrypted;
        document.getElementById('pkey_input').value = data.pkey;
      }
    }
  } catch { /* 降级明文 */ }
  document.getElementById('login_form').submit();
  return false;
}
```

---

## 5. 数据流图

```
浏览器加载登录页
    │
    ├── GET /Cube/SiteInfo → 展示版权、更新 document.title、获取 loginBg/loginLogo
    │
    └── GET /Auth/LoginConfig → 控制 Tab 显示/隐藏、是否显示"忘记密码"、渲染 OAuth 提供者列表

用户点击密码登录
    ├── GET /Auth/Challenge → 获取 pkey + publicKey
    ├── 【浏览器端】RSA-OAEP/SHA-256 加密密码
    └── POST /Auth/Login { username, password: [密文], pkey } → 成功后跳转首页

用户点击手机验证码登录
    ├── POST /Auth/SendCode { channel: "Sms", username, action: "Login" }
    └── POST /Auth/LoginByCode { username, password: 验证码, loginCategory: 1 }

用户点击邮箱验证码登录
    ├── POST /Auth/SendCode { channel: "Mail", username, action: "Login" }
    └── POST /Auth/LoginByCode { username, password: 验证码, loginCategory: 2 }

用户点击第三方登录
    └── Location: /Sso/Login/{name}[?r=returnUrl] → OAuth 流程由后端处理

用户点击注册
    └── SPA: router.push('/register')
        MVC: Bootstrap Tab 切换到 #Register 面板

用户点击忘记密码
    ├── SPA: router.push('/forgot-password')
    │     ├── Step 1: POST /Auth/SendCode { channel, username, action: "ResetPassword" }
    │     └── Step 2: POST /Auth/ResetPassword { username, code, newPassword, confirmPassword }
    │                    └── 成功 → router.push('/login')
    └── MVC: Bootstrap Tab 切换到 #ForgotPassword 面板（同上两步，通过 AJAX 完成）
```

---

## 6. 接口兼容性说明

| 接口 | 路径 | 状态 |
|------|------|------|
| 登录页配置 | `/Auth/LoginConfig` | **当前版本** |
| 登录页配置（旧） | `/Auth/Config` | **保留**，内部重定向，不建议新项目使用 |
| 站点信息 | `/Cube/SiteInfo` | **当前版本** |
| 站点信息（旧） | `/Auth/SiteInfo` | **已移除** |
| Challenge 公钥 | `/Auth/Challenge` | **当前版本** |
| 重置密码 | `/Auth/ResetPassword` | **当前版本** |
| 密码登录 | `/Auth/Login` | **当前版本**，支持 `pkey` 字段 |
| MVC 登录（保留） | `/Admin/User/Login` | **保留**，供 MVC 皮肤和 SSO 回调使用 |

- `/Auth/*` 系列接口由 `AuthController` 提供，适配 SPA 前端与 MVC 皮肤的 AJAX 调用
- `/Admin/User/*` 原有 MVC 接口保留，供 MVC 皮肤表单直传和旧版项目使用
- 两套接口共享 `UserService` 业务层，行为一致
